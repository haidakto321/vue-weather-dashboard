---
phase: 07-richer-weather-milestone-verification
plan: 05
type: execute
wave: 2
depends_on: [07-02, 07-03, 07-04]
files_modified:
  - src/components/WeatherCard.vue
  - src/pages/CityDetailPage.vue
  - src/i18n/messages/en.ts
  - src/i18n/messages/ja.ts
  - src/__tests__/weatherCard.spec.ts
  - src/__tests__/cityDetail.spec.ts
autonomous: true
requirements: [WTHR-04, WTHR-05, DATA-06]
must_haves:
  truths:
    - "CityDetailPage shows a current-conditions panel (feels-like, precipitation, UV index, sunrise, sunset) that did not exist before this plan"
    - "WeatherCard shows the same richer fields, its wind reading respects the active wind unit, and both surfaces show a last-updated time with a working manual-refresh button"
    - "A second geolocation-added city (id 0) routes to its OWN detail page, not the first geolocation city's, because routing now uses city.key"
    - "The geolocation-added city's name renders as the localized 'My Location' label everywhere its name is shown, not the raw stored string"
  artifacts:
    - path: src/pages/CityDetailPage.vue
      provides: "Current-conditions panel wired to useCurrentWeather(city), with its own loading/error/content branches"
      contains: "data-testid=\"current-conditions\""
    - path: src/components/WeatherCard.vue
      provides: "Richer fields + wind-unit formatting + last-updated/refresh + route-by-key + testid"
      contains: "data-testid=\"weather-card\""
  key_links:
    - from: src/components/WeatherCard.vue
      to: "vue-router city-detail route"
      via: ":to params.id"
      pattern: "params: \\{ id: city\\.key \\}"
    - from: src/pages/CityDetailPage.vue
      to: src/composables/useCurrentWeather.ts
      via: "useCurrentWeather(city)"
      pattern: "useCurrentWeather\\(city\\)"
---

<objective>
Deliver the richer-current-conditions UI (WTHR-04) and the last-updated/manual-refresh affordance (DATA-06) on BOTH `WeatherCard.vue` (dashboard cards) and `CityDetailPage.vue` (the detail page, which currently shows no current-conditions data at all) - plus wire the wind-unit composable into WeatherCard's wind reading (completing WTHR-05's card side; Plan 07-07 completes the Settings-page side) and fix the WeatherCard routing bug that only matters once geolocation cities exist.

Purpose: `CurrentWeather` already carries feelsLike/precipitation/uvIndex/sunrise/sunset (Plan 07-02) and `dataUpdatedAt`/`isRefetching` are already returned by every `useQuery` call for free - this plan is pure UI consumption, no new fetch logic. Both surfaces are edited together because they share the exact same composable call, the exact same new i18n keys, and the exact same rendering pattern (icon + `wmoToCondition` + `useTemperature`/`useWindSpeed` formatting) - splitting them into separate plans would mean editing `src/i18n/messages/{en,ja}.ts` twice for the same keys. The `WeatherCard` route-by-key fix (RESEARCH Pitfall 4) is bundled in here because it touches the exact same file this plan already edits extensively, and is safe to apply regardless of whether geolocation cities exist yet.
Output: a new current-conditions panel on `CityDetailPage.vue`; `WeatherCard.vue`'s content branch extended with the same richer fields, a last-updated/refresh row, `data-testid="weather-card"`, and routing by `city.key`; new `card.*`/`detail.currentHeading` i18n keys (en/ja parity); a new `weatherCard.spec.ts` component test.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@.planning/phases/07-richer-weather-milestone-verification/07-PATTERNS.md
@src/components/WeatherCard.vue
@src/pages/CityDetailPage.vue
@src/composables/useCurrentWeather.ts
@src/composables/useWindSpeed.ts
@src/lib/wmo.ts
@src/components/ForecastList.vue
@src/i18n/messages/en.ts
</context>

<interfaces>
useCurrentWeather(city: MaybeRefOrGetter<SavedCity | undefined>) (src/composables/useCurrentWeather.ts) returns the full Vue Query result object, including dataUpdatedAt: Ref<number> (epoch ms) and isRefetching: Ref<boolean> alongside the already-used data/isPending/isError/refetch - all already available with zero new code (verified against installed @tanstack/query-core types).

useWindSpeed() (Plan 07-03, src/composables/useWindSpeed.ts) returns { unit, unitSymbol, convert, format } - same shape as useTemperature, reads prefs.windUnit. format(kmh) already returns a complete unit-suffixed string such as "20 km/h" or "12 mph".

CurrentWeather (Plan 07-02, src/types/weather.ts) now has: temperature, weatherCode, windSpeed, humidity, feelsLike, precipitation, uvIndex, sunrise, sunset.

ForecastList.vue's dateLocale pattern (lines 16-20): `const dateLocale = computed(() => (locale.value === 'ja' ? 'ja-JP' : 'en-GB'))` - reuse verbatim in both files for formatting sunrise/sunset via toLocaleTimeString. Note: sunrise/sunset are ISO datetime strings WITH a time component (e.g. "2026-07-08T04:52"), so `new Date(iso)` parses them as LOCAL time already (unlike the date-ONLY string bug fixed in commit 43cf556/WR-01, which affected day-only "YYYY-MM-DD" strings) - no special local-time parsing workaround is needed here, only toLocaleTimeString formatting.

src/stores/cities.ts's cityKey() (lines 14-16): id ? String(id) : `${lat},${lon},${name}` composite - already globally unique for every city including geolocation entries (id: 0).
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Add richer-conditions i18n keys + CityDetailPage current-conditions panel</name>
  <files>src/pages/CityDetailPage.vue, src/i18n/messages/en.ts, src/i18n/messages/ja.ts, src/__tests__/cityDetail.spec.ts</files>
  <read_first>
    - src/pages/CityDetailPage.vue - full file (98 lines): the `city` computed (lines 23-26), the `useForecast(city)`/`useHourlyForecast(city)` calls (lines 37, 45), and the loading/error/content three-branch template (lines 66-94)
    - src/components/WeatherCard.vue - lines 24-29 (useCurrentWeather + wmoToCondition usage) as the rendering pattern to reuse
    - src/i18n/messages/en.ts - `card` block (lines 44-51) and `detail` block (lines 89-98)
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Pattern 3, Pattern 5, and Code Example 5 (dataUpdatedAt/lastUpdatedLabel computed)
    - .planning/phases/07-richer-weather-milestone-verification/07-PATTERNS.md - "src/pages/CityDetailPage.vue (edit: new current-conditions panel)" section
  </read_first>
  <action>
    Add these keys to src/i18n/messages/en.ts's existing `card` block (do not create a new block): feelsLike: 'Feels like {value}', precipitation: 'Precipitation: {value} mm', uvIndex: 'UV index: {value}', sunrise: 'Sunrise {value}', sunset: 'Sunset {value}', lastUpdated: 'Updated {time}', refresh: 'Refresh'. Also change the existing `wind` key from '{value} km/h' to '{value} {unit}' (WTHR-05 - the unit is now parameterized, matching the `chart.tempHigh` convention, kept for reference/consistency even though the WeatherCard/CityDetailPage call sites in this plan render wind via useWindSpeed's own format() helper directly rather than through this key). Add one new key to the `detail` block: currentHeading: 'Current conditions'. Add the identical keys to src/i18n/messages/ja.ts's `card` and `detail` blocks with natural Japanese values: feelsLike: '体感温度 {value}', precipitation: '降水量: {value} mm', uvIndex: 'UV指数: {value}', sunrise: '日の出 {value}', sunset: '日の入り {value}', lastUpdated: '最終更新: {time}', refresh: '更新', wind stays '{value} {unit}', detail.currentHeading: '現在の天気'.
    In src/pages/CityDetailPage.vue, add imports: useCurrentWeather from '@/composables/useCurrentWeather', useTemperature from '@/composables/useTemperature', useWindSpeed from '@/composables/useWindSpeed', wmoToCondition from '@/lib/wmo'. After the existing `city`/`useForecast(city)`/`useHourlyForecast(city)` declarations, add: `const { data: current, isPending: currentPending, isError: currentError, refetch: refetchCurrent, dataUpdatedAt, isRefetching } = useCurrentWeather(city)` (reusing the SAME resolved `city` computed - the untrusted route :id only ever selects a saved city, never feeds the request directly, matching the existing DATA-04/T-06-03 discipline already applied to useForecast/useHourlyForecast on this page). Add `const currentCondition = computed(() => current.value ? wmoToCondition(current.value.weatherCode) : null)`, `const { format } = useTemperature()`, `const { format: formatWind } = useWindSpeed()`, and a `dateLocale` computed copied verbatim from ForecastList.vue (locale from useI18n, already destructured on this page). Add `const lastUpdatedLabel = computed(() => dataUpdatedAt.value ? new Date(dataUpdatedAt.value).toLocaleTimeString(dateLocale.value, { hour: '2-digit', minute: '2-digit' }) : '')` and a `formatTime(iso: string)` helper doing the same toLocaleTimeString formatting for sunrise/sunset. Also add `const displayName = computed(() => city.value?.id === 0 ? t('geo.myLocation') : city.value?.name ?? '')` and replace the existing `{{ city.name }}` h1 usage (line 63) with `{{ displayName }}` (GEO-01: the geolocation entry's stored name is a static English literal, but the render site must show the localized 'My Location' label per the locked GEO-01 naming decision).
    In the template, insert a new block directly ABOVE the existing `<v-row>` forecast block (before line 80): a heading `<h2 class="text-h6 mb-2">{{ t('detail.currentHeading') }}</h2>`, then `<v-progress-circular v-if="currentPending" indeterminate color="primary" class="my-4" />`, then `<v-alert v-else-if="currentError" type="error" variant="tonal" density="compact" class="mb-4">{{ t('detail.loadError') }}<template #append><v-btn size="small" variant="text" @click="refetchCurrent()">{{ t('detail.retry') }}</v-btn></template></v-alert>`, then `<v-card v-else-if="current && currentCondition" class="mb-4" data-testid="current-conditions"><v-card-text>` containing, in order: an icon+temperature row (v-icon :icon="currentCondition.icon" next to format(current.temperature) and t(currentCondition.labelKey), mirroring WeatherCard's content branch); a row with feelsLike/precipitation/uvIndex using `t('card.feelsLike', { value: format(current.feelsLike) })`, `t('card.precipitation', { value: Math.round(current.precipitation * 10) / 10 })`, and `t('card.uvIndex', { value: Math.round(current.uvIndex) })`; a row with sunrise/sunset using `t('card.sunrise', { value: formatTime(current.sunrise) })` and `t('card.sunset', { value: formatTime(current.sunset) })`; a wind reading rendered as plain text via `formatWind(current.windSpeed)` (useWindSpeed's format already returns a complete unit-suffixed string - do not route this one through the `card.wind` i18n key); and a last-updated+refresh row identical in structure to WeatherCard's Task 2 row, built from lastUpdatedLabel/isRefetching/refetchCurrent. Close `</v-card-text></v-card>`.
    Extend src/__tests__/cityDetail.spec.ts's existing test: after the existing forecast-list/forecast-chart/hourly-chart assertions, add `const currentConditions = wrapper.find('[data-testid="current-conditions"]'); expect(currentConditions.exists()).toBe(true); expect(currentConditions.text()).toContain('19')` (feelsLike from the Plan 07-02 fixture) to prove the new panel renders using the existing mocked fetchCurrentWeather fixture.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/cityDetail.spec.ts src/__tests__/i18nParity.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - CityDetailPage.vue renders a data-testid="current-conditions" block once useCurrentWeather(city) resolves, showing feels-like/precipitation/uvIndex/sunrise/sunset/last-updated/refresh.
    - The page's h1 shows t('geo.myLocation') instead of the raw name when the resolved city has id === 0.
    - en.ts and ja.ts stay in key parity (i18nParity.spec.ts passes).
    - npx vitest run src/__tests__/cityDetail.spec.ts passes, including the new current-conditions assertion.
    - npm run build succeeds (no type errors from the new composable calls).
  </acceptance_criteria>
  <done>CityDetailPage shows a real current-conditions panel where none existed before, satisfying WTHR-04's "detail" half and DATA-06's CityDetailPage half.</done>
</task>

<task type="auto">
  <name>Task 2: Extend WeatherCard with richer fields, wind unit, last-updated/refresh, route-by-key fix, testid</name>
  <files>src/components/WeatherCard.vue, src/__tests__/weatherCard.spec.ts</files>
  <read_first>
    - src/components/WeatherCard.vue - full file (100 lines): the router-link `:to` (line 44), the useCurrentWeather destructure (line 24), the content branch (lines 79-97)
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Pitfall 4 (route-by-id collision), Pattern 5/Code Example 5 (last-updated+refresh), Pitfall 6 (card.wind hard-coded unit)
    - src/__tests__/citySearch.spec.ts - the mountSearch()-style test-harness pattern (Vuetify + Pinia + i18n) to adapt for a WeatherCard mount that ALSO needs a router (for the :to prop) and VueQueryPlugin (for useCurrentWeather)
    - src/__tests__/navigation.spec.ts - buildRouter()'s createMemoryHistory router-construction pattern (lines 43-52)
  </read_first>
  <action>
    In src/components/WeatherCard.vue, change the router-link `:to` on line 44 from `params: { id: String(city.id) }` to `params: { id: city.key }` (Pitfall 4: id:0 collides across multiple geolocation-added cities since city.key is already globally unique for every city, real or geolocated; CityDetailPage.vue already accepts `c.key === id` as a fallback match, so no other file needs to change for this fix). Add `data-testid="weather-card"` to the root v-card. Add a `displayName` computed identical in shape to the one added to CityDetailPage.vue in Task 1 (`city.id === 0 ? t('geo.myLocation') : city.name`) and replace the `{{ city.name }}` title span (line 48) with `{{ displayName }}`.
    Add imports: useWindSpeed from '@/composables/useWindSpeed'. Destructure `const { format: formatWind } = useWindSpeed()` alongside the existing `const { format } = useTemperature()`. Extend the existing `useCurrentWeather(() => props.city)` destructure (line 24) to also pull `dataUpdatedAt, isRefetching`. Add a `dateLocale` computed copied verbatim from ForecastList.vue (needs `locale` added to the existing `const { t } = useI18n()` destructure -> `const { t, locale } = useI18n()`). Add `lastUpdatedLabel` and `formatTime(iso)` computed/helper identical to the ones added in CityDetailPage.vue Task 1.
    In the content branch (lines 79-97), change the wind span (line 90) from `t('card.wind', { value: Math.round(data.windSpeed) })` to plain text `formatWind(data.windSpeed)` (useWindSpeed.format already returns a complete unit-suffixed string, e.g. "20 km/h" or "12 mph" - do not also route it through the `card.wind` i18n key). Add a new row below the existing wind/humidity row containing feelsLike/precipitation/uvIndex (same three t() calls as CityDetailPage's Task 1, reading `data.feelsLike`/`data.precipitation`/`data.uvIndex`), a second new row with sunrise/sunset (same t() calls as CityDetailPage, reading `data.sunrise`/`data.sunset` through formatTime), and a last-updated+refresh row: a `<span>{{ t('card.lastUpdated', { time: lastUpdatedLabel }) }}</span>` next to a `<v-btn :icon="isRefetching ? 'mdi-loading mdi-spin' : 'mdi-refresh'" size="x-small" variant="text" :disabled="isRefetching" :aria-label="t('card.refresh')" @click.stop.prevent="refetch()" />` (the `.stop.prevent` modifiers match the existing retry button's convention on line 72, since the card root is a router-link).
    Create src/__tests__/weatherCard.spec.ts: mock '@/lib/openMeteo' with fetchCurrentWeather.mockResolvedValue returning the Plan-07-02-extended CurrentWeather shape (temperature: 20, weatherCode: 0, windSpeed: 18, humidity: 50, feelsLike: 19, precipitation: 0.4, uvIndex: 3, sunrise: '2026-07-08T04:52', sunset: '2026-07-08T19:47'); build a mountCard(city) helper installing a createRouter/createMemoryHistory router with a single `{ path: '/city/:id', name: 'city-detail', component: { template: '<div/>' } }` route, Vuetify, a fresh Pinia, VueQueryPlugin, and the real i18n instance (same plugin list pattern as cityDetail.spec.ts's mountApp, scoped to just WeatherCard). Write three tests: (1) mount with a normal SavedCity fixture, flushPromises, assert the rendered text contains the feelsLike/precipitation/uvIndex values (via the i18n-rendered card.feelsLike etc. strings); (2) mount TWO cards representing two geolocation-added cities that share `id: 0` but have different `key`/`latitude`/`longitude` values, assert their rendered root `<a>` elements have DIFFERENT `href` attributes each ending in the respective city's `key` (proves the Pitfall 4 fix); (3) mount, flushPromises, click the refresh button (inside the `data-testid="weather-card"` root, found by its aria-label or icon), and assert `fetchCurrentWeather` was called a second time.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/weatherCard.spec.ts src/__tests__/i18nParity.spec.ts && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - WeatherCard.vue's router-link uses params.id = city.key, never String(city.id).
    - Two WeatherCard instances for two different geolocation-added cities (both id: 0) render distinct hrefs.
    - WeatherCard's wind reading changes when the active windUnit preference changes (uses useWindSpeed, not a hard-coded km/h string).
    - A last-updated label and a working refresh button render in the content branch (not just the error branch).
    - npx vitest run src/__tests__/weatherCard.spec.ts passes all three new tests; npm run test (full suite) and npm run build stay green.
  </acceptance_criteria>
  <done>WeatherCard shows the same richer current-conditions fields as CityDetailPage, respects the active wind unit, offers a manual refresh with a last-updated label, routes correctly for geolocation cities, and carries data-testid="weather-card" for the Plan 07-08 e2e test.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 05 scope)

- src/pages/CityDetailPage.vue: new current-conditions panel (`data-testid="current-conditions"`), `currentCondition`/`lastUpdatedLabel`/`displayName` computeds, `formatTime` helper
- src/components/WeatherCard.vue: richer content branch (feelsLike/precipitation/uvIndex/sunrise/sunset), last-updated/refresh row, `data-testid="weather-card"`, route-by-`city.key`, `displayName` computed
- src/i18n/messages/en.ts / ja.ts: `card.feelsLike`, `card.precipitation`, `card.uvIndex`, `card.sunrise`, `card.sunset`, `card.lastUpdated`, `card.refresh`, `detail.currentHeading`; `card.wind` changed to `'{value} {unit}'`
- src/__tests__/weatherCard.spec.ts (new file)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| vue-router :id param -> city lookup | untrusted route param is only ever used to find an existing SavedCity, never to build a request (unchanged discipline, now keyed by city.key too) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-ROUTE | Tampering / Spoofing | WeatherCard route-by-id collision across geolocation cities | mitigate | Route by city.key (globally unique for every city) instead of city.id (0 for every geolocation entry); regression-tested with two geolocation cities sharing id:0 asserting distinct hrefs. |
| T-07-XSS2 | Tampering | displayName / sunrise / sunset / feelsLike interpolated into templates | accept | All values render via Vue template interpolation (auto-escaped) or t() message interpolation, never v-html; sunrise/sunset are Open-Meteo's own response strings, not user input. |
</threat_model>

<verification>
- Automated gates in both tasks cover: panel rendering, routing regression, wind-unit reactivity, refresh wiring, i18n parity, full-suite/build regression.
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): visually confirm the new fields/icons look reasonable in both light/dark theme and both languages, and that the refresh spinner animates.
</verification>

<success_criteria>
- WTHR-04 is fully implemented on both cards and detail; DATA-06 is fully implemented on both surfaces; WTHR-05's WeatherCard-side wiring is complete (Settings-side wiring is Plan 07-07); the Pitfall 4 routing bug is fixed before Plan 07-06 introduces multiple geolocation cities.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-05-SUMMARY.md` when done.
</output>
