---
phase: 05-refactor-hardening
plan: 03
type: execute
wave: 3
depends_on: [05-01, 05-02]
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [I18N-03]
must_haves:
  truths:
    - "package.json declares vue-i18n ^11.4.6 (dependencies) and msw ^2.14.6 (devDependencies), and the lockfile resolves both from registry.npmjs.org"
    - "npm install output contains no 'deprecated' notice for vue-i18n"
    - "The full test suite is green on vue-i18n v11 with src/i18n/index.ts and src/main.ts byte-identical (all v1.0 language behavior intact)"
    - "No msw artifact exists in public/ and no msw import exists outside src/__tests__"
  artifacts:
    - path: package.json
      provides: "Upgraded vue-i18n + new msw dev dependency"
      contains: "\"vue-i18n\": \"^11"
    - path: package-lock.json
      provides: "Pinned resolution of both packages to the official npm registry"
  key_links:
    - from: src/i18n/index.ts
      to: vue-i18n v11
      via: "unchanged createI18n({ legacy: false, ... }) - v11-compatible as-is"
      pattern: "legacy: false"
---

<objective>
Upgrade vue-i18n from the npm-deprecated v9 line to v11 (I18N-03) and install msw as a dev dependency (needed by plan 05-04 for TEST-04) - both gated behind a blocking human package-legitimacy checkpoint required by the RESEARCH.md Package Legitimacy Audit.

Purpose: v9 is npm-deprecated; v11 is the maintained line. The RESEARCH breaking-change audit confirmed this codebase uses none of the removed APIs (legacy mode, tc/$tc, v-t, allowComposition, modulo interpolation), so the upgrade is a package.json change verified by the existing suite.
Output: updated package.json + package-lock.json, verified green suite on v11, msw available for the test plan.

Sequenced after plans 01-02 on purpose: npm must not mutate node_modules while other executors run tests (Windows file locks), and running the finished refactor's suite against v11 is the cleanest "behavior intact" signal.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refactor-hardening/05-RESEARCH.md
@package.json
@src/i18n/index.ts
@src/main.ts
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate - approve msw install and vue-i18n upgrade</name>
  <read_first>
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - "Package Legitimacy Audit" section (full seam-verdict analysis)
    - .planning/REQUIREMENTS.md - "Approved New Dependencies" (msw is user-approved for this milestone)
  </read_first>
  <action>
    Present the audit summary below to the developer and WAIT for an explicit reply. Do not run any install command and do not modify package.json or the lockfile until the developer answers "approved". This gate is NEVER auto-approvable: workflow.auto_advance is ignored for package legitimacy checkpoints.
  </action>
  <what-built>
    Nothing installed yet - this is the pre-install supply-chain gate the RESEARCH audit requires. The legitimacy heuristic flagged both packages:
    - msw 2.14.6: flagged for its postinstall script. RESEARCH analysis: heuristic false positive - the package dates to 2018, has ~16.9M weekly downloads, lives at github.com/mswjs/msw, and its postinstall only imports a script bundled inside the package (`node -e "import('./config/scripts/postinstall.js').catch(() => void 0)"`, verified via npm registry). It is also a user-approved dependency named in REQUIREMENTS.md.
    - vue-i18n 11.4.6: flagged "too-new" (released 2026-06-18 on the actively maintained intlify line, github.com/intlify/vue-i18n).
    Per protocol the flags stand and this checkpoint blocks until a human decides.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/msw - confirm repository is github.com/mswjs/msw, weekly downloads in the millions, publisher is the MSW team.
    2. Open https://www.npmjs.com/package/vue-i18n - confirm repository is github.com/intlify/vue-i18n and 11.4.6 is the latest of the v11 line.
    3. Optional: `npm view msw scripts.postinstall` locally and confirm it matches the internal-script-only command quoted above.
  </how-to-verify>
  <verify>
    <automated>NONE - blocking human gate: the pass condition is the developer's explicit "approved" reply; no command can substitute for it</automated>
  </verify>
  <acceptance_criteria>
    - The developer explicitly approved both installs (or raised concerns and execution stopped here).
    - No install ran and no package.json/package-lock.json change happened before that approval.
  </acceptance_criteria>
  <done>Developer approval recorded in the execution log; Task 2 unblocked.</done>
  <resume-signal>Type "approved" to proceed with the installs in Task 2, or describe concerns to stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install vue-i18n@^11.4.6 and msw@^2.14.6 (dev); verify I18N-03</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - src/i18n/index.ts (createI18n({ legacy: false, locale, fallbackLocale: 'en', messages }) - expected UNCHANGED by this upgrade)
    - src/main.ts (.use(i18n) - expected UNCHANGED)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - "State of the Art" vue-i18n migration specifics and Pitfall 7
  </read_first>
  <action>
    Run the two installs as separate commands, capturing output: `npm install vue-i18n@^11.4.6` then `npm install -D msw@^2.14.6`. Node engine gate already verified: vue-i18n@11 needs Node >= 22, local machine runs v22.14.0.
    Constraints:
    - Do NOT run `npx msw init` (that emits public/mockServiceWorker.js for browser mocking - Phase 5 uses msw/node only, nothing MSW-related may enter the app bundle or public/).
    - Do NOT install @intlify/unplugin-vue-i18n (not approved, not needed - JIT message compilation is the v10+ default and the messages are build-imported TS objects).
    - src/i18n/index.ts and src/main.ts are expected byte-identical after this task. If either seems to need a change, STOP and re-check the RESEARCH breaking-change audit (all v10/v11 breaks hit Legacy API mode, tc/$tc, v-t, allowComposition, modulo interpolation - grep-verified absent from this codebase) before touching them.
    - Pitfall 7: the esm-bundler feature-flag console hint is an optimization suggestion, NOT a deprecation warning - do not chase it and do not add Vite define flags (optional polish explicitly skipped; flag names were unverified assumption A1).
    Verify after install: install output has no "deprecated" line for vue-i18n; lockfile entries for both packages resolve to registry.npmjs.org tarballs; full suite green; production build clean.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!/\^11\./.test(p.dependencies['vue-i18n'])) process.exit(1); if(!p.devDependencies.msw) process.exit(1); const l=require('./package-lock.json'); const m=l.packages['node_modules/msw'], v=l.packages['node_modules/vue-i18n']; if(!m.resolved.startsWith('https://registry.npmjs.org/msw/')) process.exit(1); if(!v.resolved.startsWith('https://registry.npmjs.org/vue-i18n/')) process.exit(1)" && npm run test && npm run build && test ! -f public/mockServiceWorker.js && test -z "$(git status --porcelain src/i18n/index.ts src/main.ts)"</automated>
  </verify>
  <acceptance_criteria>
    - package.json: vue-i18n at ^11.4.6 in dependencies; msw at ^2.14.6 in devDependencies (never in dependencies).
    - package-lock.json resolves both packages from registry.npmjs.org (node gate above).
    - `npm install` output contained no "deprecated" notice for vue-i18n (paste the relevant output lines into the summary).
    - `npm run test` fully green and `npm run build` clean on v11; no deprecation warnings in test output (`npm run test 2>&1 | grep -i deprecat` finds nothing vue-i18n related).
    - src/i18n/index.ts and src/main.ts unmodified (git status empty for both); public/ contains no mockServiceWorker.js.
  </acceptance_criteria>
  <done>App runs on vue-i18n v11 with all v1.0 language behavior intact; msw is available for plan 05-04.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 03 scope)

- package.json: `"vue-i18n": "^11.4.6"` (dependencies), `"msw": "^2.14.6"` (devDependencies)
- package-lock.json: updated resolutions for both packages (committed with the dep change per RESEARCH Runtime State Inventory)
- No source-code symbols: src/i18n/index.ts, src/main.ts, and all message files are verified unchanged

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry -> node_modules | third-party code enters the dev machine and (for vue-i18n) the app bundle |
| devDependencies -> production bundle | test tooling must never ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-SC | Tampering (supply chain) | npm installs of msw + vue-i18n | mitigate | Blocking human-verify checkpoint (Task 1, never auto-approvable) before any install; verify registry pages resolve to mswjs/msw and intlify/vue-i18n; lockfile resolution gate in Task 2; lockfile committed with the change. |
| T-05-07 | Elevation of Privilege | msw entering the app bundle | mitigate | devDependency only; no `msw init`; automated gate: no public/mockServiceWorker.js; msw imports will exist only under src/__tests__ (enforced in plan 05-04). |
| T-05-08 | Tampering | vue-i18n message rendering post-upgrade | accept | Messages are static, author-controlled TS objects; vue-i18n interpolation renders as text; no v-html in translated content. Low risk, no code change. |
</threat_model>

<verification>
- Automated gate in Task 2 covers: versions, lockfile provenance, green suite, clean build, no msw browser artifact, untouched i18n bootstrap files.
- Human check (deferred to end-of-phase verification per workflow.human_verify_mode=end-of-phase): run the dev server, switch en/ja in Settings, confirm all copy switches and the browser console shows no deprecation warning.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 5 (i18n half) satisfied: app runs on vue-i18n v11 with no deprecation warnings and all v1.0 language behavior intact.
- Requirement I18N-03 implemented and recorded in frontmatter; msw staged for TEST-04.
</success_criteria>

<output>
Create `.planning/phases/05-refactor-hardening/05-03-SUMMARY.md` when done.
</output>
