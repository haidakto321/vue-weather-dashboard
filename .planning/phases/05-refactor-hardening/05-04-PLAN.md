---
phase: 05-refactor-hardening
plan: 04
type: execute
wave: 4
depends_on: [05-02, 05-03]
files_modified:
  - src/__tests__/openMeteo.spec.ts
  - src/__tests__/citySearch.spec.ts
autonomous: true
requirements: [TEST-04, TEST-05]
must_haves:
  truths:
    - "npm run test runs MSW-mocked openMeteo tests covering success, empty-result, and error shapes - green with zero real network calls"
    - "CitySearch tests prove debounce (exactly one geocode call 300ms after the last keystroke), abort-on-new-input (first AbortSignal aborted), and select-clears-field"
    - "The whole suite (old + new specs) is green in one npm run test invocation"
  artifacts:
    - path: src/__tests__/openMeteo.spec.ts
      provides: "API-layer MSW tests in a node environment"
      contains: "@vitest-environment node"
      min_lines: 40
    - path: src/__tests__/citySearch.spec.ts
      provides: "CitySearch component tests with fake timers"
      contains: "advanceTimersByTimeAsync"
      min_lines: 60
  key_links:
    - from: src/__tests__/openMeteo.spec.ts
      to: src/lib/openMeteo.ts
      via: "REAL imports (no vi.mock) so MSW exercises the actual HTTP boundary"
      pattern: "from '@/lib/openMeteo'"
    - from: src/__tests__/openMeteo.spec.ts
      to: "msw/node"
      via: "setupServer with unhandled requests erroring"
      pattern: "onUnhandledRequest: 'error'"
    - from: src/__tests__/citySearch.spec.ts
      to: src/components/CitySearch.vue
      via: "VAutocomplete update:search emit drives onSearch (the contract SRCH-04 hardened)"
      pattern: "update:search"
---

<objective>
Lock down the API layer and the refactored search behavior with tests: MSW-mocked node-environment tests for `openMeteo.ts` covering success, empty-result, and error shapes (TEST-04), and jsdom component tests for CitySearch covering debounce, abort-on-new-input, and select-clears-field (TEST-05).

Purpose: these tests are the phase's regression lock - the API layer's real URL/param/error serialization gets exercised for the first time (MSW's one obvious job), and the SRCH-04 debounce rework is pinned by behavior tests.
Output: two new spec files; full suite green.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refactor-hardening/05-RESEARCH.md
@.planning/phases/05-refactor-hardening/05-PATTERNS.md
@src/lib/openMeteo.ts
@src/components/CitySearch.vue
@src/__tests__/cityDetail.spec.ts
</context>

## Conventions that apply to every task

- Copy the codebase test-file comment style: an explanatory comment above mock/setup blocks (see cityDetail.spec.ts). Never use the em-dash character; use "-".
- MSW v2 syntax ONLY (`http`, `HttpResponse` from 'msw'; `setupServer` from 'msw/node') - never v1 `rest.*`.
- msw imports are allowed ONLY inside src/__tests__ (threat T-05-10); nothing MSW-related enters src/ app code or public/.
- Do NOT create `src/__tests__/msw/handlers.ts` unless real reuse emerges while writing - per-test `server.use()` overrides are the default pattern (RESEARCH Code Example 5).

<tasks>

<task type="auto">
  <name>Task 1: MSW node-environment tests for openMeteo.ts (TEST-04)</name>
  <files>src/__tests__/openMeteo.spec.ts</files>
  <read_first>
    - src/lib/openMeteo.ts (the two base URLs at lines ~9-10; the `?? []` empty-result fallback; the snake_case -> camelCase normalization in fetchCurrentWeather and fetchForecast; every function's optional AbortSignal param)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - Code Example 5 (the verified template - no codebase analog exists) and Pitfall 5
    - src/__tests__/cityDetail.spec.ts (comment style only)
  </read_first>
  <action>
    Create `src/__tests__/openMeteo.spec.ts`. FIRST line must be the docblock `// @vitest-environment node` (overrides the config-wide jsdom; axios then uses its http adapter, MSW's most reliable interception path - Pitfall 5). Import the REAL `geocodeCity`, `fetchCurrentWeather`, `fetchForecast` from '@/lib/openMeteo' - no vi.mock anywhere in this file. Setup per RESEARCH Code Example 5: `const server = setupServer()`, `beforeAll(() => server.listen({ onUnhandledRequest: 'error' }))`, `afterEach(() => server.resetHandlers())`, `afterAll(() => server.close())`. Intercept the exact URLs from openMeteo.ts: geocoding search endpoint and forecast endpoint.
    Required coverage (the three shapes from TEST-04), asserting against the REAL normalization logic read from openMeteo.ts:
    1. Success: geocodeCity sends the search term as an encoded `name` query param and returns the normalized city array; fetchCurrentWeather and fetchForecast return camelCase-normalized objects from snake_case API payloads (e.g. temperature_2m -> temperature, daily.time -> dates - confirm exact field names by reading the file, and build realistic response fixtures matching Open-Meteo's shape).
    2. Empty result: geocodeCity resolves to [] when the response JSON has no `results` key.
    3. Error shapes: an HTTP 500 rejects with an axios error exposing `response.status` 500; a network error (`HttpResponse.error()`) rejects.
    Do NOT test the 10s axios timeout (real-time wait; explicitly out of scope per research). Add the codebase-style explanatory comment above the server setup explaining MSW's role (intercepts Node http, so tests exercise real URL/param/error serialization with zero live network).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/openMeteo.spec.ts && head -1 src/__tests__/openMeteo.spec.ts | grep -q "@vitest-environment node" && ! grep -q "vi.mock" src/__tests__/openMeteo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - Spec runs in the node environment (first-line docblock) and passes.
    - Covers all three shapes: success (param serialization + normalization), empty result ([]), error (HTTP status rejection + network error rejection).
    - Uses real openMeteo imports and MSW v2 handlers with onUnhandledRequest: 'error' (any live request fails the test loudly).
  </acceptance_criteria>
  <done>openMeteo.ts is covered at the real HTTP boundary; TEST-04 satisfied.</done>
</task>

<task type="auto">
  <name>Task 2: CitySearch component tests - debounce, abort, select (TEST-05)</name>
  <files>src/__tests__/citySearch.spec.ts</files>
  <read_first>
    - src/__tests__/cityDetail.spec.ts (the harness to copy wholesale: vi.mock block for '@/lib/openMeteo' at lines ~20-35, ResizeObserver/matchMedia jsdom shims at lines ~49-70, mount pattern with fresh Pinia + Vuetify + i18n plugins at lines ~76-94)
    - src/components/CitySearch.vue (post-refactor: the @update:search -> onSearch contract, onSelect behavior, which store action a selection calls, how the field resets)
    - src/stores/cities.ts (the store API onSelect uses - assert against it)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - Code Example 6 and Pitfall 4 (fake timers + microtask flushing)
  </read_first>
  <action>
    Create `src/__tests__/citySearch.spec.ts` (jsdom - the config default; no environment docblock). Copy the cityDetail.spec.ts harness: `vi.mock('@/lib/openMeteo', () => ({ geocodeCity: vi.fn().mockResolvedValue([]), fetchCurrentWeather: vi.fn(), fetchForecast: vi.fn() }))` (mock ALL exports), the ResizeObserver + matchMedia shim block verbatim, fresh `createPinia()` + `setActivePinia` per test, `createVuetify({ components, directives })`, and mount CitySearch DIRECTLY with `global.plugins: [vuetify, pinia, i18n]` (no router, no VueQuery - CitySearch uses neither). Timer discipline (Pitfall 4): `vi.useFakeTimers()` in beforeEach, `vi.useRealTimers()` in afterEach; advance with `await vi.advanceTimersByTimeAsync(300)` then `await flushPromises()` - never real sleeps.
    Drive the component through its contract: `wrapper.findComponent({ name: 'VAutocomplete' }).vm.$emit('update:search', ...)` (avoids fragile DOM typing through Vuetify's overlay).
    Three required tests:
    1. Debounce: emit 'update:search' twice quickly ('Lon' then 'Lond'); assert geocodeCity NOT called; advance 300ms + flush; assert called exactly once with first arg 'Lond'.
    2. Abort-on-new-input: fire one search and advance so geocodeCity is called (have the mock return a never-resolving promise for this test); capture `vi.mocked(geocodeCity).mock.calls[0][1]` (the AbortSignal); fire a second search and advance past the debounce; assert the captured `signal.aborted === true`.
    3. Select-clears-field: emit 'update:model-value' with a GeoCity fixture; assert the cities store received the city (per the actual store API read from cities.ts) and the search field/model was reset (assert the observable reset the component performs - read onSelect first).
    Add the codebase-style comment above the mock block explaining why the module mock (component tests stub the HTTP layer; MSW is only for the API-layer spec).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/citySearch.spec.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - All three behaviors covered and green: single debounced call with the latest term, first call's AbortSignal aborted after a newer input, selection updates the store and clears the field.
    - Fake timers with the async advance API; real timers restored in afterEach (no arbitrary sleeps anywhere).
    - Full suite (`npm run test`) green in one run - old and new specs together.
  </acceptance_criteria>
  <done>SRCH-04 behavior is pinned by tests; TEST-05 satisfied; whole Phase 5 suite green.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 04 scope)

- `src/__tests__/openMeteo.spec.ts` - node-env MSW spec; test names covering: geocode success + param encoding, current-weather/forecast normalization, empty-result [], HTTP 500 rejection, network-error rejection
- `src/__tests__/citySearch.spec.ts` - jsdom component spec; test names covering: debounce single-call-with-latest-term, abort-on-new-input signal.aborted, select-clears-field + store update
- No app-code symbols: this plan writes tests only

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Test process -> network | tests must never reach the real Open-Meteo API |
| Test code -> app bundle | mocking tools must stay out of production code |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-09 | Information Disclosure / DoS (flakiness) | openMeteo.spec.ts HTTP boundary | mitigate | `server.listen({ onUnhandledRequest: 'error' })` fails loudly on any unmocked request - zero live traffic. |
| T-05-10 | Elevation of Privilege | msw leaking into the app | mitigate | msw imports confined to src/__tests__ (grep gate below); plan 03 already gated public/ and the build output. |
</threat_model>

<verification>
- `npm run test` fully green (all pre-existing specs plus the two new ones).
- Containment gate: `grep -rl "msw" src --include='*.ts' --include='*.vue' | grep -v "^src/__tests__/"` returns nothing (msw exists only under src/__tests__).
- Human check (deferred to end-of-phase verification per workflow.human_verify_mode=end-of-phase): none specific to this plan beyond the whole-phase walkthrough; the tests ARE the verification artifact.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 4 (test half) satisfied: CitySearch tests cover debounce, abort-on-new-input, and select-clears-field.
- ROADMAP criterion 5 (test half) satisfied: openMeteo.ts passes MSW-mocked tests for success, empty-result, and error shapes.
- Requirements TEST-04 and TEST-05 implemented and recorded in frontmatter.
</success_criteria>

<output>
Create `.planning/phases/05-refactor-hardening/05-04-SUMMARY.md` when done.
</output>
