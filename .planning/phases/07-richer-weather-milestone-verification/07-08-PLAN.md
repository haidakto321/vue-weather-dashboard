---
phase: 07-richer-weather-milestone-verification
plan: 08
type: execute
wave: 4
depends_on: [07-01, 07-05, 07-06, 07-07]
files_modified:
  - playwright.config.ts
  - tsconfig.e2e.json
  - e2e/smoke.spec.ts
  - src/components/CitySearch.vue
  - package.json
  - .gitignore
autonomous: true
requirements: [TEST-06]
must_haves:
  truths:
    - "npx playwright test passes a smoke flow: search a city, see its card appear, open its detail page, and see the forecast list plus both charts render"
    - "Playwright's own project files are excluded from the app's type-check scope (npm run build) and from git-ignored artifact directories"
  artifacts:
    - path: playwright.config.ts
      provides: "webServer (npm run dev) + single chromium project config"
      contains: "webServer"
    - path: e2e/smoke.spec.ts
      provides: "The milestone-closing smoke test"
      contains: "city-search"
  key_links:
    - from: e2e/smoke.spec.ts
      to: "src/App.vue via the Vite dev server"
      via: "playwright.config.ts webServer block"
      pattern: "command: 'npm run dev'"
---

<objective>
Deliver TEST-06: a Playwright end-to-end smoke test that drives a real browser against the real running app - search a city, see its card, open the detail page, see the forecast list and both charts render - closing out the v1.1 milestone's testing requirements.

Purpose: every other Phase 5-7 test runs against jsdom (Vitest/Vue Test Utils); this is the first test that exercises a real browser, a real Vite dev server, and the real Open-Meteo API together, verifying the whole stack actually works end to end rather than in simulation. It runs last in this phase (after 07-05/07-06/07-07) so the app it tests against is feature-complete and stable.
Output: playwright.config.ts, tsconfig.e2e.json (kept OUT of the root tsconfig's project-reference graph per RESEARCH Pitfall 9, so npm run build scope is unchanged), e2e/smoke.spec.ts, one new data-testid attribute (city-search on CitySearch - weather-card was already added in Plan 07-05), a test:e2e npm script, and .gitignore entries for Playwright's local artifact directories.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@src/components/CitySearch.vue
@tsconfig.json
@tsconfig.node.json
@package.json
</context>

<interfaces>
Existing data-testid convention already present and unchanged by this plan: forecast-list (ForecastList.vue line 49), forecast-chart (ForecastChart.vue), hourly-chart (HourlyChart.vue line 115), weather-card (WeatherCard.vue, added in Plan 07-05). This plan adds exactly one more: city-search on CitySearch.vue's v-autocomplete.

Root tsconfig.json (3 lines of references) currently references only tsconfig.app.json and tsconfig.node.json - this plan creates tsconfig.e2e.json but does NOT add it to that references array (RESEARCH Pitfall 9: keeps npm run build's scope unchanged; e2e correctness is validated by npx playwright test, not vue-tsc).

package.json scripts block currently has dev/build/preview/lint/test - this plan adds "test:e2e": "playwright test".
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Create playwright.config.ts + tsconfig.e2e.json + package.json script + gitignore</name>
  <files>playwright.config.ts, tsconfig.e2e.json, package.json, .gitignore</files>
  <read_first>
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Code Example 6 (playwright.config.ts, verified against playwright.dev docs) and Pitfall 9 (tsconfig.e2e.json must NOT be added to the root tsconfig references)
    - tsconfig.node.json - full file (24 lines), the shape tsconfig.e2e.json mirrors (compilerOptions block + include array)
    - tsconfig.json - full file (7 lines), confirm its references array stays untouched
    - vite.config.ts - confirm no dev-server port override (Playwright's baseURL assumes the default 5173)
    - .gitignore - full file, current ignore categories
  </read_first>
  <action>
    Create playwright.config.ts at the project root: import defineConfig and devices from '@playwright/test'; export a defineConfig with testDir set to './e2e', fullyParallel: true, retries set to 1 when process.env.CI is set else 0, a use block with baseURL 'http://localhost:5173' and trace 'on-first-retry', a projects array with one entry named 'chromium' spreading devices['Desktop Chrome'] (a single Chromium project only - no cross-browser matrix needed for one smoke test), and a webServer block with command 'npm run dev', url 'http://localhost:5173', reuseExistingServer set to true when process.env.CI is unset, and timeout 30000.
    Create tsconfig.e2e.json mirroring tsconfig.node.json's compilerOptions shape (target ESNext, module ESNext, moduleResolution bundler, lib ESNext, strict mode flags - copy tsconfig.node.json's compilerOptions block verbatim except tsBuildInfoFile, which should read "./node_modules/.tmp/tsconfig.e2e.tsbuildinfo"), with an include array of ["playwright.config.ts", "e2e/**/*.ts"]. Do NOT add this file to tsconfig.json's references array - leave tsconfig.json completely unmodified, so npm run build (vue-tsc --build && vite build) never type-checks e2e/ or playwright.config.ts.
    In package.json's scripts block, add a new "test:e2e" entry with value "playwright test" directly after the existing "test": "vitest run" entry.
    In .gitignore, add a new section after the existing "Vitest / tsc build info" section with four new ignore entries: /test-results/, /playwright-report/, /blob-report/, /playwright/.cache/ (Playwright's local run-artifact directories, never committed).
  </action>
  <verify>
    <automated>node -e "require('./tsconfig.e2e.json')" && node -e "const p=require('./package.json'); if(p.scripts['test:e2e']!=='playwright test') process.exit(1)" && node -e "const t=require('./tsconfig.json'); if(t.references.some(r=>r.path.includes('e2e'))) process.exit(1)" && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - playwright.config.ts exports a config with testDir './e2e', a chromium-only projects array, and a webServer pointed at npm run dev on port 5173.
    - tsconfig.e2e.json is valid JSON and is NOT referenced from tsconfig.json.
    - package.json has a test:e2e script equal to "playwright test".
    - .gitignore excludes Playwright's local artifact directories.
    - npm run build still succeeds and its scope is unchanged (no e2e type-check added).
  </acceptance_criteria>
  <done>Playwright is fully configured against the real Vite dev server, ready for Task 2's smoke test.</done>
</task>

<task type="auto">
  <name>Task 2: Add city-search testid + write the smoke test + run it</name>
  <files>src/components/CitySearch.vue, e2e/smoke.spec.ts</files>
  <read_first>
    - src/components/CitySearch.vue - full file (136 lines), the v-autocomplete root element (lines 119-134)
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Code Example 7 (full smoke.spec.ts, verified structure), Pitfall 7 (Vuetify autocomplete needs auto-waiting locators, never manual waitForTimeout), Pitfall 8 (why city-search/weather-card testids are needed)
    - src/pages/DashboardPage.vue - confirm the route path is '/' and WeatherCard's route-by-key link (Plan 07-05/07-06) is what the smoke test navigates through
  </read_first>
  <action>
    In src/components/CitySearch.vue, add `data-testid="city-search"` to the v-autocomplete element (the same additive, low-risk pattern already used for unit-toggle/theme-switch/language-toggle/weather-card - it does not change any existing test or behavior).
    Create e2e/smoke.spec.ts: import test and expect from '@playwright/test'. Write one test 'search a city, see its card, open the detail page, see forecast + charts render': page.goto('/'); locate the city-search testid element, click it, then fill it with 'London'; locate an option via page.getByRole('option', { name: /London/ }).first(), await its waitFor(), then click it (auto-waiting locators only - no manual page.waitForTimeout sleeps, per Pitfall 7, since Vuetify's autocomplete overlay plus the 300ms search debounce already need the default 5s auto-wait to settle); locate the weather-card testid element filtered by text containing 'London', assert it is visible; click that card; assert the forecast-list testid element is visible, the forecast-chart testid element is visible, and the hourly-chart testid element is visible.
    Run `npx playwright test` locally to confirm the smoke flow passes against the real dev server and the real Open-Meteo API (no mocking - this is the one deliberately real, non-mocked test in the whole suite).
  </action>
  <verify>
    <automated>npx playwright test</automated>
  </verify>
  <acceptance_criteria>
    - CitySearch.vue's v-autocomplete carries data-testid="city-search".
    - e2e/smoke.spec.ts uses only auto-waiting Playwright locators (getByRole/getByTestId with default timeouts) - no manual page.waitForTimeout calls anywhere in the file.
    - npx playwright test passes the full search -> card -> detail -> forecast-list + forecast-chart + hourly-chart flow against the real dev server.
  </acceptance_criteria>
  <done>TEST-06 is fully satisfied: the milestone-closing Playwright smoke test passes against the real running app.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 08 scope)

- playwright.config.ts (new file): testDir, chromium project, webServer pointed at npm run dev
- tsconfig.e2e.json (new file): typed-lint/e2e-only tsconfig, deliberately unreferenced from the root
- e2e/smoke.spec.ts (new file): the search -> card -> detail -> forecast+charts smoke test
- src/components/CitySearch.vue: `data-testid="city-search"` on the v-autocomplete
- package.json: `test:e2e` script
- .gitignore: Playwright artifact-directory ignores

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Playwright browser process -> real Open-Meteo API | the smoke test is the one place in the suite that makes a real, unmocked network call |
| devDependencies -> production bundle | @playwright/test and the e2e test code must never ship in dist/ |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-E2EBUNDLE | Elevation of Privilege / supply chain | @playwright/test and e2e/ code shipping in the production bundle | mitigate | @playwright/test stays a devDependency (verified in Plan 07-01); tsconfig.e2e.json and e2e/ are excluded from the Vite build entirely (no import path from src/ into e2e/, confirmed by grepping dist/ output after npm run build contains no 'playwright' string). |
| T-07-SECRET | Information Disclosure | playwright.config.ts / e2e/smoke.spec.ts containing secrets or tokens | accept | Neither file contains any credential, API key, or token - Open-Meteo requires none and the config only references localhost URLs. |
| T-07-PERM | (non-STRIDE) | Geolocation permission prompts during automated e2e runs | accept | The smoke test's scope (search -> card -> detail -> forecast/chart) never triggers the GeolocationButton, so no permission prompt is exercised in this automated flow; GEO-01 is human-verified separately per Plan 07-06. |
</threat_model>

<verification>
- Automated gate in Task 1 covers: config validity, script wiring, build-scope isolation.
- Automated gate in Task 2 covers: the full real-browser smoke flow (this IS the automated verification - no further human check is layered on top for TEST-06 itself).
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): none additional required - the Playwright run is itself the strongest available automated proof for this requirement.
</verification>

<success_criteria>
- TEST-06 is satisfied: `npx playwright test` passes the milestone-closing smoke flow against the real app.
- This is the final plan of Phase 7 - all six phase requirements (GEO-01, STATE-04, WTHR-04, WTHR-05, DATA-06, TEST-06) are now fully implemented and independently verified.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-08-SUMMARY.md` when done.
</output>
