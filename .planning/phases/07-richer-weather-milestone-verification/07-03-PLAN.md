---
phase: 07-richer-weather-milestone-verification
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/types/preferences.ts
  - src/stores/preferences.ts
  - src/composables/useWindSpeed.ts
  - src/__tests__/preferences.store.spec.ts
  - src/__tests__/useWindSpeed.spec.ts
autonomous: true
requirements: [WTHR-05]
must_haves:
  truths:
    - "A windUnit preference axis exists (kmh/mph), defaults to kmh, and is sanitized on read-back exactly like unit/theme/language"
    - "useWindSpeed converts km/h to mph and formats with the active unit symbol, mirroring useTemperature's contract exactly"
    - "A tampered weather-prefs localStorage entry with an invalid windUnit falls back to kmh, never crashing or leaking a bad value"
  artifacts:
    - path: src/types/preferences.ts
      provides: "WindUnit type + WIND_UNITS + windUnit field on Preferences/DEFAULT_PREFERENCES"
      contains: "WindUnit = 'kmh' | 'mph'"
    - path: src/stores/preferences.ts
      provides: "windUnit getter + setWindUnit action + sanitize branch"
      contains: "setWindUnit"
    - path: src/composables/useWindSpeed.ts
      provides: "kmhToMph + useWindSpeed() returning { unit, unitSymbol, convert, format }"
      contains: "export function useWindSpeed"
  key_links:
    - from: src/composables/useWindSpeed.ts
      to: src/stores/preferences.ts
      via: "prefs.windUnit computed read"
      pattern: "prefs\\.windUnit"
---

<objective>
Add the wind-unit preference axis (kmh/mph) to the preferences type and store, and create `useWindSpeed` as a direct structural mirror of `useTemperature` - the composable that will let `WeatherCard` (Plan 07-05) and `SettingsPage` (Plan 07-07) show/switch wind speed units.

Purpose: this is pure foundation - no UI wiring yet. Building it as its own plan keeps the data-layer contract (`windUnit`, `useWindSpeed`) stable and independently testable before two later plans (07-05, 07-07) consume it, avoiding a repeat edit to `src/types/preferences.ts`/`src/stores/preferences.ts` from multiple directions.
Output: `WindUnit` type, `windUnit` field wired through the same `useLocalStorage`-backed preferences store as `unit`/`theme`/`language`, and a new `useWindSpeed` composable with the exact same `{ unit, unitSymbol, convert, format }` return contract as `useTemperature`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@.planning/phases/07-richer-weather-milestone-verification/07-PATTERNS.md
@src/types/preferences.ts
@src/stores/preferences.ts
@src/composables/useTemperature.ts
</context>

<interfaces>
Full src/composables/useTemperature.ts (34 lines) is the exact structural template useWindSpeed.ts must mirror:
celsiusToFahrenheit(c) is a pure, exported, unit-testable conversion function.
useTemperature() reads usePreferencesStore(), derives unit/unitSymbol as computed(), exposes convert(celsius) and format(celsius), and returns { unit, unitSymbol, convert, format }.

Full src/types/preferences.ts (37 lines): TemperatureUnit/TEMPERATURE_UNITS is the pattern WindUnit/WIND_UNITS mirrors; Preferences interface and DEFAULT_PREFERENCES both need a windUnit field added.

src/stores/preferences.ts sanitize() function (lines 23-36): each axis has an allow-list check against DEFAULT_PREFERENCES fallback - windUnit needs an identical branch using WIND_UNITS.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the WindUnit type + Preferences field + store sanitize/getter/setter</name>
  <files>src/types/preferences.ts, src/stores/preferences.ts, src/__tests__/preferences.store.spec.ts</files>
  <read_first>
    - src/types/preferences.ts - full file, especially TemperatureUnit (line 11), TEMPERATURE_UNITS (line 35), Preferences interface (lines 20-24), DEFAULT_PREFERENCES (lines 27-31)
    - src/stores/preferences.ts - full file, especially the sanitize() unit branch (lines 26-28), the unit getter (line 54), and setUnit (lines 59-61)
    - src/__tests__/preferences.store.spec.ts - full file, especially "falls back to the default unit when localStorage holds an invalid unit" (lines 49-57) as the pattern to mirror for windUnit
  </read_first>
  <behavior>
    - DEFAULT_PREFERENCES.windUnit is 'kmh'.
    - store.windUnit reads the persisted value; store.setWindUnit('mph') updates it and persists to the 'weather-prefs' localStorage key (flush: 'sync', same as unit/theme/language).
    - A tampered weather-prefs entry with windUnit: 'knots' (or any value outside WIND_UNITS) sanitizes to 'kmh' on store creation, never crashing.
  </behavior>
  <action>
    In src/types/preferences.ts, add `export type WindUnit = 'kmh' | 'mph'` directly after the existing TemperatureUnit type, and `export const WIND_UNITS = ['kmh', 'mph'] as const` directly after TEMPERATURE_UNITS. Add `windUnit: WindUnit` to the Preferences interface and `windUnit: 'kmh'` to DEFAULT_PREFERENCES, in each case placed alongside the existing `unit` field for readability.
    In src/stores/preferences.ts, import `WIND_UNITS` alongside the existing `TEMPERATURE_UNITS, THEME_MODES, LANGUAGES` import, and `WindUnit` alongside the existing `TemperatureUnit, ThemeMode, Language` type import. In `sanitize()`, add a `windUnit` branch identical in shape to the existing `unit` branch: check `(WIND_UNITS as readonly string[]).includes(source.windUnit as string)`, else fall back to `DEFAULT_PREFERENCES.windUnit`. Add `const windUnit = computed(() => prefs.value.windUnit)` alongside the existing `unit` getter, and `function setWindUnit(w: WindUnit) { prefs.value.windUnit = w }` alongside `setUnit`. Add `windUnit` and `setWindUnit` to the store's returned object.
    In src/__tests__/preferences.store.spec.ts, add three tests mirroring the existing unit tests: "starts from DEFAULT_PREFERENCES" gains a `windUnit` assertion (expect(store.windUnit).toBe(DEFAULT_PREFERENCES.windUnit)); add a new "setWindUnit updates the wind unit" test calling store.setWindUnit('mph') and asserting store.windUnit === 'mph'; add a new "falls back to the default wind unit when localStorage holds an invalid windUnit" test seeding localStorage with a tampered `{ unit: 'celsius', theme: 'light', language: 'en', windUnit: 'knots' }` object before store creation and asserting store.windUnit === DEFAULT_PREFERENCES.windUnit.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/preferences.store.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - src/types/preferences.ts exports WindUnit and WIND_UNITS; Preferences and DEFAULT_PREFERENCES both include windUnit.
    - src/stores/preferences.ts's sanitize() rejects any windUnit value not in WIND_UNITS, falling back to 'kmh'.
    - store returns both windUnit (computed) and setWindUnit (function) from the returned object.
    - npx vitest run src/__tests__/preferences.store.spec.ts passes all preferences-store tests including the three new/extended windUnit assertions.
  </acceptance_criteria>
  <done>The preferences store persists a validated windUnit exactly like unit/theme/language, ready for useWindSpeed and the Settings UI to consume.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create useWindSpeed composable mirroring useTemperature</name>
  <files>src/composables/useWindSpeed.ts, src/__tests__/useWindSpeed.spec.ts</files>
  <read_first>
    - src/composables/useTemperature.ts - full file (34 lines), the exact structure to mirror
    - src/__tests__/useTemperature.spec.ts - full file, the exact test structure to mirror for useWindSpeed
  </read_first>
  <behavior>
    - kmhToMph(0) === 0; kmhToMph(100) is approximately 62.1371 (exact: 100 * 0.621371).
    - useWindSpeed().format(20) === '20 km/h' when windUnit is 'kmh' (the default).
    - After prefs.setWindUnit('mph'), useWindSpeed().format(20) === '12 mph' (Math.round(20 * 0.621371) = 12).
    - useWindSpeed() is reactive to a windUnit change on the store, same as useTemperature is reactive to unit changes.
  </behavior>
  <action>
    Create src/composables/useWindSpeed.ts as a direct structural mirror of useTemperature.ts: export a pure `kmhToMph(kmh: number): number` returning `kmh * 0.621371` (exported the same way celsiusToFahrenheit is, unit-testable without a store). Export `useWindSpeed()` that calls usePreferencesStore(), derives `unit = computed(() => prefs.windUnit)` and `unitSymbol = computed(() => (unit.value === 'mph' ? 'mph' : 'km/h'))`, defines `convert(kmh: number): number` returning `unit.value === 'mph' ? kmhToMph(kmh) : kmh`, defines `format(kmh: number): string` returning the rounded convert() value plus a space plus unitSymbol (note the space - this differs from useTemperature's format, which has no space before °C/°F, because "20°C" reads naturally but "20kmh" does not - use "20 km/h"), and returns `{ unit, unitSymbol, convert, format }` - the same four-key shape as useTemperature so call sites can use either composable identically.
    Create src/__tests__/useWindSpeed.spec.ts mirroring useTemperature.spec.ts's structure exactly: a `beforeEach` clearing localStorage and setting an active Pinia; a test asserting kmhToMph's exact conversion at 0 and 100; a test asserting format(20) defaults to '20 km/h'; a test asserting format(20) becomes '12 mph' after prefs.setWindUnit('mph'); a test asserting reactivity (call useWindSpeed() once, assert the default format, call setWindUnit, assert format() changes without re-calling useWindSpeed()).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/useWindSpeed.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - useWindSpeed() returns exactly { unit, unitSymbol, convert, format }, matching useTemperature's return shape.
    - kmhToMph is exported and unit-testable without a Pinia store, matching celsiusToFahrenheit's pattern.
    - npx vitest run src/__tests__/useWindSpeed.spec.ts passes all four new tests.
  </acceptance_criteria>
  <done>useWindSpeed exists as a drop-in structural sibling to useTemperature, ready for WeatherCard (Plan 07-05) and the Settings wind toggle (Plan 07-07) to import.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 03 scope)

- src/types/preferences.ts: `WindUnit` type, `WIND_UNITS` const, `windUnit` field on `Preferences` and `DEFAULT_PREFERENCES`
- src/stores/preferences.ts: `windUnit` computed getter, `setWindUnit(w: WindUnit)` action, `windUnit` sanitize branch
- src/composables/useWindSpeed.ts (new file): `kmhToMph(kmh: number): number`, `useWindSpeed(): { unit, unitSymbol, convert, format }`
- src/__tests__/useWindSpeed.spec.ts (new file)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| localStorage (weather-prefs) -> store | user/tamper-controllable JSON is read back into typed preference state |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-PREF | Tampering | windUnit read-back from localStorage | mitigate | sanitize() allow-list check against WIND_UNITS, identical to the existing unit/theme/language branches (threat T-04-01 precedent); a corrupt value falls back to 'kmh', never crashes hydration. |
</threat_model>

<verification>
- Automated gates in both tasks cover: type/store correctness, sanitize fallback, conversion math, composable reactivity.
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): none needed yet - no UI exposes windUnit until Plans 07-05/07-07.
</verification>

<success_criteria>
- windUnit is a fully validated, persisted preference axis with the same guarantees as unit/theme/language.
- useWindSpeed is a verified drop-in mirror of useTemperature, ready for two later plans to consume without further data-layer work.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-03-SUMMARY.md` when done.
</output>
