---
phase: 07-richer-weather-milestone-verification
plan: 06
type: execute
wave: 2
depends_on: [07-01, 07-04]
files_modified:
  - src/stores/cities.ts
  - src/pages/DashboardPage.vue
  - src/__tests__/cities.store.spec.ts
  - src/__tests__/dashboardPage.spec.ts
autonomous: true
requirements: [GEO-01, STATE-04]
must_haves:
  truths:
    - "Dragging a city card to a new position persists that order across a reload (the cities store's localStorage-backed array reflects the new order)"
    - "A 'Use my location' button appears on the dashboard next to city search, and clicking it can add a current-location card"
  artifacts:
    - path: src/stores/cities.ts
      provides: "reorderCities(newOrder) action"
      contains: "function reorderCities"
    - path: src/pages/DashboardPage.vue
      provides: "draggable grid + GeolocationButton wiring"
      contains: "<draggable"
  key_links:
    - from: src/pages/DashboardPage.vue
      to: src/stores/cities.ts
      via: "@update:model-value=\"store.reorderCities\""
      pattern: "store\\.reorderCities"
    - from: src/pages/DashboardPage.vue
      to: src/components/GeolocationButton.vue
      via: "<GeolocationButton />"
      pattern: "GeolocationButton"
---

<objective>
Wire STATE-04 (drag-and-drop city reorder via vuedraggable) and GEO-01's dashboard entry point (GeolocationButton, built in Plan 07-04) into DashboardPage.vue, plus add the reorderCities store action that persists the new order.

Purpose: vuedraggable@^4.1.0 (Plan 07-01) and GeolocationButton.vue/useMyLocation (Plan 07-04) are both already-built, independently-tested pieces - this plan is the wiring step that assembles them into the dashboard page a user actually sees. reorderCities mirrors addCity/removeCity's existing whole-array-reassignment style so persistence is free (the cities ref is already a useLocalStorage ref with flush: 'sync').
Output: stores/cities.ts gains reorderCities; DashboardPage.vue's card grid becomes a draggable wrapper and gains a GeolocationButton above the city list; new/extended tests prove both the persistence behavior and the page wiring.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@.planning/phases/07-richer-weather-milestone-verification/07-PATTERNS.md
@src/stores/cities.ts
@src/pages/DashboardPage.vue
@src/components/GeolocationButton.vue
</context>

<interfaces>
GeolocationButton.vue (Plan 07-04, src/components/GeolocationButton.vue): a self-contained component with no required props - a bare usage with no props is a complete usage. Exposes data-testid="geo-button" on its root button.

vuedraggable@4.1.0 (Plan 07-01) API (verified against the published README): a draggable wrapper takes :model-value bound to the array, an item-key prop naming the key field, a tag prop (use "div", never a Vuetify component object), an @update:model-value handler, and a named #item slot receiving { element } for each row. The default export from the 'vuedraggable' package is this draggable component.

src/stores/cities.ts current addCity/removeCity (lines 47-65): both reassign cities.value wholesale (push/filter), which is what makes useLocalStorage's watcher persist automatically - reorderCities follows the identical style.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add reorderCities store action</name>
  <files>src/stores/cities.ts, src/__tests__/cities.store.spec.ts</files>
  <read_first>
    - src/stores/cities.ts - full file (69 lines), especially addCity (lines 47-60) and removeCity (lines 62-65) as the exact style to mirror, and the returned object on line 67
    - src/__tests__/cities.store.spec.ts - full file, especially "removes a city by key" (lines 48-56) as the structural test pattern
  </read_first>
  <behavior>
    - store.reorderCities([tokyo, london]) when the store currently holds [london, tokyo] (in that order) results in store.cities being exactly [tokyo, london], in that new order.
    - After reorderCities, the weather-cities localStorage key reflects the new order immediately (flush: 'sync', same as addCity/removeCity).
  </behavior>
  <action>
    In src/stores/cities.ts, add a new function reorderCities(newOrder: SavedCity[]) directly below removeCity, with a one-line body `cities.value = newOrder` and a comment matching the file's existing comment style (e.g. "Replace the whole order - useLocalStorage's watcher persists it the same way addCity's push and removeCity's filter already do"). Add reorderCities to the store's returned object on line 67, alongside cities, hasCities, addCity, removeCity.
    In src/__tests__/cities.store.spec.ts, add a new test "reorderCities replaces the city order and persists it": seed the store with london then tokyo via addCity (initial order [london, tokyo]), call store.reorderCities with the two entries swapped (new order [tokyo, london]), assert store.cities[0].name is 'Tokyo' and store.cities[1].name is 'London', then read back localStorage.getItem('weather-cities'), JSON.parse it, and assert the parsed array's first entry name is 'Tokyo' (mirrors preferences.store.spec.ts's "persists a setter change" test pattern).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/cities.store.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - reorderCities is exported from useCitiesStore's returned object.
    - Calling reorderCities with a permuted array replaces store.cities in that exact order.
    - The persisted weather-cities localStorage entry reflects the new order immediately after the call.
    - npx vitest run src/__tests__/cities.store.spec.ts passes all cities-store tests including the new reorderCities test.
  </acceptance_criteria>
  <done>The cities store exposes a reorderCities action that persists drag-and-drop order exactly like addCity/removeCity persist their changes.</done>
</task>

<task type="auto">
  <name>Task 2: Wire draggable grid + GeolocationButton into DashboardPage</name>
  <files>src/pages/DashboardPage.vue, src/__tests__/dashboardPage.spec.ts</files>
  <read_first>
    - src/pages/DashboardPage.vue - full file (40 lines), especially the existing v-row/v-col loop (lines 33-37) and the storeToRefs destructure (lines 10-11)
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Pattern 2 and Code Example 2 (draggable grid wiring, tag="div" + v-col inside #item, avoiding the tag-as-component edge case)
    - src/__tests__/navigation.spec.ts - buildRouter()'s createMemoryHistory router-construction pattern (lines 43-52), reused here since WeatherCard's router-link requires an installed router
    - src/__tests__/cityDetail.spec.ts - the vi.mock('@/lib/openMeteo') fixture pattern (lines 20-42), reused/adapted here since DashboardPage renders WeatherCard which calls useCurrentWeather
  </read_first>
  <action>
    In src/pages/DashboardPage.vue, import `draggable` from 'vuedraggable' and `GeolocationButton` from '@/components/GeolocationButton.vue'. Add `<GeolocationButton class="mb-4" />` in the template directly after `<CitySearch class="mb-6" />` and before the empty-state/city-list block, so a user can either search or use their current location. Replace the existing `<v-row v-else><v-col v-for="city in cities" :key="city.key" cols="12" sm="6" md="4"><WeatherCard :city="city" /></v-col></v-row>` block with a `<draggable v-if="hasCities" :model-value="cities" item-key="key" tag="div" class="d-flex flex-wrap ga-4" @update:model-value="store.reorderCities">` wrapper containing a `<template #item="{ element }"><v-col cols="12" sm="6" md="4" class="pa-0" style="min-width: 280px"><WeatherCard :city="element" /></v-col></template>` (per RESEARCH Pattern 2/Pitfall 4's verified-safe approach - tag="div" plus a v-col inside the #item slot, never a Vuetify component object passed as the tag prop). Keep the existing storeToRefs(store) destructuring of cities/hasCities (lines 10-11) unchanged; bind @update:model-value to store.reorderCities directly on the store instance (an action call, not a storeToRefs ref).
    Create src/__tests__/dashboardPage.spec.ts: mock '@/lib/openMeteo' the same way cityDetail.spec.ts does (fetchCurrentWeather resolving a fixed CurrentWeather fixture with all nine Plan-07-02 fields, geocodeCity resolving []); build a mountDashboard() helper installing a createRouter/createMemoryHistory router with a `{ path: '/city/:id', name: 'city-detail', component: { template: '<div/>' } }` route (needed for WeatherCard's :to prop), Vuetify, a fresh Pinia, VueQueryPlugin, and the real i18n instance, mounting DashboardPage.vue directly (no App.vue chrome needed). Write two tests: (1) seed two cities via the cities store before mount (e.g. london and tokyo), mount, flushPromises, assert `wrapper.findAll('[data-testid="weather-card"]')` has length 2 in the seeded order; (2) with the same setup, call `store.reorderCities([...cities reversed])` directly on the store after mount, await nextTick, and assert the rendered weather-card elements now appear in the reversed order (proves the template's #item slot is bound reactively to the store's cities array, which is what the draggable's @update:model-value handler also drives).
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/dashboardPage.spec.ts && npx vitest run src/__tests__/navigation.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - DashboardPage.vue renders a GeolocationButton between CitySearch and the city grid.
    - DashboardPage.vue's city grid is wrapped in a draggable component bound to the cities store via item-key="key" and @update:model-value="store.reorderCities".
    - npx vitest run src/__tests__/dashboardPage.spec.ts passes both new tests.
    - The pre-existing src/__tests__/navigation.spec.ts still passes unmodified (regression check - the draggable wrapper must not break the existing Dashboard-renders-initially / swaps-page assertions).
    - npm run build succeeds (vuedraggable's types resolve cleanly under strict mode).
  </acceptance_criteria>
  <done>DashboardPage.vue offers both a geolocation entry point and drag-and-drop reorder, with the reorder persisting through the already-tested reorderCities action.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 06 scope)

- src/stores/cities.ts: `reorderCities(newOrder: SavedCity[])` action
- src/pages/DashboardPage.vue: `<draggable>`-wrapped city grid, `<GeolocationButton>` above the grid
- src/__tests__/dashboardPage.spec.ts (new file)

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Drag gesture -> localStorage order | user-driven reordering writes directly into the persisted weather-cities array |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-REORDER | Tampering | reorderCities reassigning the persisted cities array | accept | reorderCities reassigns the SAME already-validated SavedCity array (no new deserialization path); the existing isValidSavedCity read-back validator in cities.ts already rejects malformed entries on the next store creation; drag-and-drop order itself carries no executable content. |
| T-07-A11Y | (non-STRIDE, accessibility note) | Drag-and-drop has no keyboard-accessible fallback | accept | Explicitly out of scope per the locked STATE-04 accessibility decision for this phase - SortableJS/vuedraggable is pointer/touch-only and no fallback is built here. |
</threat_model>

<verification>
- Automated gates in both tasks cover: store persistence, page wiring, reactive re-render on reorder, regression against the existing navigation test and build.
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): manually drag two cards and confirm the visual order survives a page reload; click "Use my location" and confirm a card appears (browser will prompt for permission).
</verification>

<success_criteria>
- STATE-04 is fully implemented: drag-and-drop reorder persists across reload.
- GEO-01's dashboard entry point is live: the button is visible and wired to the already-tested useMyLocation composable.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-06-SUMMARY.md` when done.
</output>
