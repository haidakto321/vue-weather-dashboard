---
phase: 07-richer-weather-milestone-verification
plan: 07
type: execute
wave: 3
depends_on: [07-03, 07-05]
files_modified:
  - src/pages/SettingsPage.vue
  - src/i18n/messages/en.ts
  - src/i18n/messages/ja.ts
  - src/__tests__/settingsPage.spec.ts
autonomous: true
requirements: [WTHR-05]
must_haves:
  truths:
    - "User can switch wind speed unit (km/h / mph) in Settings, and the choice persists across reload and applies wherever wind is shown (WeatherCard/CityDetailPage, wired in Plan 07-05)"
  artifacts:
    - path: src/pages/SettingsPage.vue
      provides: "Wind-unit v-btn-toggle control"
      contains: "data-testid=\"wind-unit-toggle\""
  key_links:
    - from: src/pages/SettingsPage.vue
      to: src/stores/preferences.ts
      via: "store.setWindUnit"
      pattern: "setWindUnit"
---

<objective>
Add a wind-speed-unit toggle to `SettingsPage.vue`, completing WTHR-05 (the composable and store field were built in Plan 07-03; WeatherCard/CityDetailPage already consume `useWindSpeed` as of Plan 07-05).

Purpose: this is the last piece of WTHR-05 - a UI control the user actually interacts with. It is its own plan because `SettingsPage.vue` and the `card`/`settings` i18n keys it needs are otherwise untouched by any other Phase 7 plan, and it depends on Plan 07-05 only for i18n-file wave sequencing (both plans edit `src/i18n/messages/{en,ja}.ts`).
Output: a new `v-btn-toggle` control on SettingsPage mirroring the existing temperature-unit toggle exactly, plus three new `settings.*` i18n keys (en/ja parity).
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@src/pages/SettingsPage.vue
@src/stores/preferences.ts
@src/i18n/messages/en.ts
</context>

<interfaces>
usePreferencesStore() (Plan 07-03, src/stores/preferences.ts) now returns windUnit (computed, 'kmh' | 'mph') and setWindUnit(w: WindUnit) alongside the existing unit/theme/language getters/setters.

SettingsPage.vue's existing unit v-btn-toggle block (lines 20-23, 58-75) is the exact structural template to mirror: a computed options array reading t() labels, an onChange handler with a value-narrowing `if (value === 'x' || value === 'y')` guard calling the store setter, and a v-card/v-card-title/v-card-text/v-btn-toggle block with a data-testid.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add wind-unit toggle to SettingsPage + i18n keys</name>
  <files>src/pages/SettingsPage.vue, src/i18n/messages/en.ts, src/i18n/messages/ja.ts, src/__tests__/settingsPage.spec.ts</files>
  <read_first>
    - src/pages/SettingsPage.vue - full file (114 lines), especially the unit computed/onUnitChange (lines 20-30) and the unit v-card block (lines 58-75)
    - src/i18n/messages/en.ts - `settings` block (lines 104-114)
    - src/__tests__/settingsPage.spec.ts - full file, especially "changing the unit control updates the preferences store" (lines 118-133) as the exact test pattern to mirror
  </read_first>
  <behavior>
    - The Settings page renders a data-testid="wind-unit-toggle" control alongside the existing unit/theme/language controls.
    - Clicking the mph option calls store.setWindUnit('mph'); store.windUnit becomes 'mph' and persists to localStorage.
  </behavior>
  <action>
    Add three keys to src/i18n/messages/en.ts's `settings` block: windUnitSection: 'Wind speed unit', kmh: 'km/h', mph: 'mph'. Add the identical keys to src/i18n/messages/ja.ts's `settings` block: windUnitSection: '風速の単位', kmh: 'km/h', mph: 'mph' (unit symbols stay as-is across locales, matching how °C/°F already read the same way conceptually).
    In src/pages/SettingsPage.vue, import `WindUnit` alongside the existing `TemperatureUnit, Language` type import from '@/types/preferences'. Add `windUnit` to the existing `const { unit, theme, language } = storeToRefs(store)` destructure. Add a `windUnitOptions` computed mirroring `unitOptions` exactly: `[{ value: 'kmh', label: t('settings.kmh') }, { value: 'mph', label: t('settings.mph') }]`, typed as `{ value: WindUnit; label: string }[]`. Add `onWindUnitChange(value: unknown)` mirroring `onUnitChange` exactly: `if (value === 'kmh' || value === 'mph') { store.setWindUnit(value) }`. In the template, add a new `<v-card class="mb-4">` block (placed directly after the existing temperature-unit v-card, before the theme v-card) with a `<v-card-title>{{ t('settings.windUnitSection') }}</v-card-title>` and a `<v-card-text>` containing a `<v-btn-toggle data-testid="wind-unit-toggle" :model-value="windUnit" color="primary" mandatory density="comfortable" @update:model-value="onWindUnitChange">` with a `<v-btn v-for="opt in windUnitOptions" :key="opt.value" :value="opt.value">{{ opt.label }}</v-btn>` inside, matching the existing unit toggle's structure exactly.
    In src/__tests__/settingsPage.spec.ts, extend the "renders the unit, theme, and language controls without throwing" test to also assert `wrapper.find('[data-testid="wind-unit-toggle"]').exists()` is true. Add a new test "changing the wind-unit control updates the preferences store" mirroring "changing the unit control updates the preferences store" (lines 118-133) exactly: assert `store.windUnit` starts at 'kmh', find the wind-unit-toggle, find its button containing text 'mph', trigger a click, and assert `store.windUnit` becomes 'mph'.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/settingsPage.spec.ts src/__tests__/i18nParity.spec.ts && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - SettingsPage.vue renders a data-testid="wind-unit-toggle" v-btn-toggle bound to the preferences store's windUnit.
    - Clicking the mph button calls store.setWindUnit('mph') and the store's windUnit becomes 'mph'.
    - en.ts and ja.ts stay in key parity (i18nParity.spec.ts passes).
    - npx vitest run src/__tests__/settingsPage.spec.ts passes all settings-page tests including the two new/extended wind-unit assertions.
    - npm run build succeeds.
  </acceptance_criteria>
  <done>WTHR-05 is fully implemented: the wind-unit preference is switchable in Settings, persists, and (via Plan 07-05) already applies wherever wind speed is shown.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 07 scope)

- src/pages/SettingsPage.vue: `windUnitOptions` computed, `onWindUnitChange` handler, new `data-testid="wind-unit-toggle"` v-btn-toggle
- src/i18n/messages/en.ts / ja.ts: `settings.windUnitSection`, `settings.kmh`, `settings.mph`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| UI control -> preferences store | user-driven selection is narrowed to the WindUnit union before reaching the store setter |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SETTINGS | Tampering | onWindUnitChange narrowing an `unknown` v-btn-toggle value | mitigate | Explicit `value === 'kmh' || value === 'mph'` guard before calling setWindUnit, identical to the existing onUnitChange pattern - any unexpected value is silently ignored, never forwarded to the store. |
</threat_model>

<verification>
- Automated gate covers: control rendering, store wiring, i18n parity, build.
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): visually confirm the toggle renders correctly in both light/dark theme and both languages.
</verification>

<success_criteria>
- WTHR-05 requirement fully satisfied end to end: switchable, persisted, and applied wherever wind speed is displayed.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-07-SUMMARY.md` when done.
</output>
