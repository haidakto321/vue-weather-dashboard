---
phase: 07-richer-weather-milestone-verification
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
autonomous: false
requirements: [STATE-04, TEST-06]
must_haves:
  truths:
    - "vuedraggable resolves to the Vue-3-compatible 4.1.0 major (the `next` dist-tag), never the Vue-2 2.24.3 `latest` tag"
    - "@playwright/test is installed as a devDependency and its Chromium browser binary is downloaded and runnable"
    - "No install command ran and no package.json/lockfile change happened before the developer explicitly approved the legitimacy checkpoint"
  artifacts:
    - path: package.json
      provides: "vuedraggable ^4.1.0 (dependencies) + @playwright/test ^1.61.1 (devDependencies)"
      contains: "\"vuedraggable\": \"^4.1.0\""
    - path: package-lock.json
      provides: "Pinned resolutions for both packages from registry.npmjs.org"
  key_links: []
---

<objective>
Install the two phase-7 approved dependencies - `vuedraggable@^4.1.0` (STATE-04 drag-and-drop) and `@playwright/test` dev-only (TEST-06 e2e) - and download the Playwright Chromium browser binary, gated behind a blocking human package-legitimacy checkpoint per the RESEARCH.md Package Legitimacy Audit.

Purpose: `vuedraggable`'s npm `latest` tag is a Vue-2 build (2.24.3) - a bare `npm install vuedraggable` installs the wrong major version, so the correct `^4.1.0` pin must land before any code imports it (Plan 07-06). `@playwright/test` was flagged `SUS` by the legitimacy heuristic (a `too-new` false positive on an official, 42.5M-weekly-download Microsoft package) and per the Phase 05-03 precedent requires an explicit human-verify gate before install. Neither package is imported by any application code until later plans, so this plan is pure dependency setup.
Output: package.json/package-lock.json updated with both packages at the correct versions; Chromium browser binary present locally for Plan 07-08's `npx playwright test`.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@package.json
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Package legitimacy gate - approve vuedraggable and @playwright/test installs</name>
  <read_first>
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - "Package Legitimacy Audit" section (full seam-verdict table and analysis)
    - .planning/REQUIREMENTS.md - "Approved New Dependencies" (both packages are user-approved for this milestone)
  </read_first>
  <action>
    Present the audit summary below to the developer and WAIT for an explicit reply. Do not run any install command and do not modify package.json or the lockfile until the developer answers "approved". This gate is NEVER auto-approvable: workflow.auto_advance is ignored for package legitimacy checkpoints, matching the Phase 05-03 precedent.
  </action>
  <what-built>
    Nothing installed yet - this is the pre-install supply-chain gate the RESEARCH audit requires.
    - vuedraggable: passed the legitimacy heuristic cleanly (published long ago, 1.33M weekly downloads, github.com/SortableJS/Vue.Draggable). Its own risk is NOT legitimacy but version selection: the npm `latest` dist-tag resolves to 2.24.3 (a Vue 2 build); the Vue 3 build is published under the `next` dist-tag as 4.1.0. The install command MUST read `vuedraggable@^4.1.0`, never a bare `vuedraggable`.
    - @playwright/test: flagged `SUS` by the heuristic on a pure `too-new` signal (most recent release published one day before this research session). RESEARCH analysis: this is a heuristic false positive - the package has 42.5M weekly downloads, lives at github.com/microsoft/playwright, and is a user-approved dependency named explicitly in REQUIREMENTS.md.
    Per protocol both flags stand until a human decides.
  </what-built>
  <how-to-verify>
    1. Open https://www.npmjs.com/package/vuedraggable - confirm the repository is github.com/SortableJS/Vue.Draggable and that version 4.1.0 exists (check the "Versions" tab or `npm view vuedraggable versions`).
    2. Open https://www.npmjs.com/package/@playwright/test - confirm the repository is github.com/microsoft/playwright and weekly downloads are in the tens of millions.
    3. Optional: `npm view vuedraggable dist-tags --json` locally and confirm `latest` is 2.x and `next` is 4.x, matching the audit.
  </how-to-verify>
  <verify>
    <automated>NONE - blocking human gate: the pass condition is the developer's explicit "approved" reply; no command can substitute for it</automated>
  </verify>
  <acceptance_criteria>
    - The developer explicitly approved both installs (or raised concerns and execution stopped here).
    - No install ran and no package.json/package-lock.json change happened before that approval.
  </acceptance_criteria>
  <done>Developer approval recorded in the execution log; Task 2 unblocked.</done>
  <resume-signal>Type "approved" to proceed with the installs in Task 2, or describe concerns to stop.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Install vuedraggable@^4.1.0, @playwright/test (dev), and the Chromium browser binary</name>
  <files>package.json, package-lock.json</files>
  <read_first>
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - "Standard Stack" Installation block and "Environment Availability" table (Chromium binary is NOT cached locally this session)
    - package.json - current dependencies/devDependencies blocks (verify no existing `vuedraggable` or `@playwright/test` entry before running)
  </read_first>
  <action>
    Run three commands in order, capturing output: `npm install vuedraggable@^4.1.0` (dependencies - NEVER a bare `npm install vuedraggable`, that resolves to the Vue-2 `latest` tag 2.24.3), then `npm install -D @playwright/test@^1.61.1` (devDependencies only - this package must never appear in `dependencies`), then `npx playwright install chromium` (downloads the Chromium browser binary; no cache exists locally per the RESEARCH Environment Availability table, so this step needs network access and may take a few minutes).
    Constraints:
    - Do not run `npx playwright install` without the `chromium` argument (a single-browser install keeps scope minimal per RESEARCH Pattern 6 - no cross-browser matrix needed for one smoke test).
    - Do not add `sortablejs` as a direct dependency - it is a transitive dependency of `vuedraggable@4.1.0` and must not be listed separately.
    - No application code changes in this task - `vuedraggable` and `@playwright/test` remain unimported until Plans 07-06 and 07-08.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if(!/\^4\.1/.test(p.dependencies.vuedraggable||'')) process.exit(1); if(!p.devDependencies['@playwright/test']) process.exit(1); const l=require('./package-lock.json'); const d=l.packages['node_modules/vuedraggable'], pw=l.packages['node_modules/@playwright/test']; if(!d.resolved.startsWith('https://registry.npmjs.org/vuedraggable/')) process.exit(1); if(!pw.resolved.startsWith('https://registry.npmjs.org/@playwright/test/')) process.exit(1)" && npx playwright --version && npm run test && npm run build</automated>
  </verify>
  <acceptance_criteria>
    - package.json: `vuedraggable` at `^4.1.0` in `dependencies` (never `^2.x`); `@playwright/test` at `^1.61.1` in `devDependencies` (never in `dependencies`).
    - package-lock.json resolves both packages from `registry.npmjs.org` (automated gate above).
    - `npx playwright --version` prints a version string (confirms the CLI and the downloaded Chromium binary are usable).
    - `npm run test` and `npm run build` stay green/clean - installing these two packages must not change any existing behavior (neither is imported yet).
  </acceptance_criteria>
  <done>vuedraggable@^4.1.0 and @playwright/test are installed with Chromium downloaded; Plans 07-06 and 07-08 can import/run them.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 01 scope)

- package.json: `"vuedraggable": "^4.1.0"` (dependencies), `"@playwright/test": "^1.61.1"` (devDependencies)
- package-lock.json: updated resolutions for both packages plus `vuedraggable`'s transitive `sortablejs@1.14.0`
- Local Chromium browser binary (Playwright browser cache) - no repo file, but required for `npx playwright test` in Plan 07-08
- No application source symbols created or modified in this plan

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| npm registry -> node_modules | third-party code enters the dev machine and (for vuedraggable) the app bundle |
| devDependencies -> production bundle | @playwright/test and its browser automation code must never ship |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-SC | Tampering (supply chain) | npm installs of vuedraggable + @playwright/test | mitigate | Blocking human-verify checkpoint (Task 1, never auto-approvable) before any install; verify registry pages resolve to SortableJS/Vue.Draggable and microsoft/playwright; lockfile resolution gate in Task 2; lockfile committed with the change. |
| T-07-VER | Tampering | vuedraggable version confusion (Vue-2 `latest` vs Vue-3 `next`) | mitigate | Install command explicitly pins `vuedraggable@^4.1.0`; automated gate asserts `^4.1` in package.json, never a bare install. |
| T-07-BUNDLE | Elevation of Privilege | @playwright/test entering the app bundle | mitigate | devDependency only; automated gate checks `devDependencies['@playwright/test']` exists and is never duplicated in `dependencies`; further verified against `dist/` output in Plan 07-08. |
</threat_model>

<verification>
- Automated gate in Task 2 covers: versions, lockfile provenance, Chromium binary usability, unchanged green suite/build.
- Human check (deferred to end-of-phase verification per workflow.human_verify_mode=end-of-phase): none additional needed beyond Task 1's approval - nothing user-visible changes yet.
</verification>

<success_criteria>
- Both approved dependencies are installed at the correct major versions, gated behind an explicit human approval.
- Chromium is downloaded and ready for Plan 07-08's e2e run.
- Requirements STATE-04 and TEST-06 have their dependency prerequisite satisfied; functional implementation continues in Plans 07-06 and 07-08.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-01-SUMMARY.md` when done.
</output>
