---
phase: 07-richer-weather-milestone-verification
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/lib/openMeteo.ts
  - src/types/weather.ts
  - src/__tests__/openMeteo.spec.ts
  - src/__tests__/cityDetail.spec.ts
autonomous: true
requirements: [WTHR-04]
must_haves:
  truths:
    - "fetchCurrentWeather requests apparent_temperature, precipitation, and uv_index in current, plus sunrise/sunset in daily, in ONE HTTP call"
    - "CurrentWeather exposes feelsLike, precipitation, uvIndex, sunrise, sunset alongside the existing temperature/weatherCode/windSpeed/humidity fields"
    - "Every test that mocks fetchCurrentWeather's return value reflects the extended shape, so no downstream UI reads undefined for the new fields"
  artifacts:
    - path: src/lib/openMeteo.ts
      provides: "Extended fetchCurrentWeather + ForecastResponse (current+daily combined request)"
      contains: "apparent_temperature,precipitation,uv_index"
    - path: src/types/weather.ts
      provides: "Extended CurrentWeather interface"
      contains: "uvIndex: number"
  key_links:
    - from: src/lib/openMeteo.ts
      to: "Open-Meteo /v1/forecast API"
      via: "http.get params object (current + daily in one request)"
      pattern: "daily: 'sunrise,sunset'"
---

<objective>
Extend fetchCurrentWeather and the CurrentWeather type with the richer current-conditions data WTHR-04 needs (feels-like, precipitation, UV index, sunrise, sunset), fetched in the SAME single HTTP call the app already makes for current weather - no new round trip.

Purpose: this is the data-layer foundation both the WeatherCard richer-fields work and the new CityDetailPage current-conditions panel (Plan 07-05) depend on. Doing it first, in isolation, keeps the data contract stable before any UI consumes it and lets the existing MSW/mock-based tests catch shape regressions immediately.
Output: src/lib/openMeteo.ts and src/types/weather.ts updated; openMeteo.spec.ts proves the new request params and response mapping; cityDetail.spec.ts's fetchCurrentWeather fixture updated to the extended shape so it stays a valid stand-in for later UI tests.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md
@.planning/phases/07-richer-weather-milestone-verification/07-PATTERNS.md
@src/lib/openMeteo.ts
@src/types/weather.ts
</context>

<interfaces>
Current CurrentWeather shape (src/types/weather.ts, lines 27-33) that this plan extends:
export interface CurrentWeather { temperature: number; weatherCode: number; windSpeed: number; humidity: number }

Current fetchCurrentWeather (src/lib/openMeteo.ts, lines 58-79) that this plan extends - same shared http client (line 7) and FORECAST_URL constant (line 10) as fetchForecast/fetchHourlyForecast.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Extend fetchCurrentWeather + CurrentWeather with feels-like, precipitation, UV index, sunrise, sunset</name>
  <files>src/lib/openMeteo.ts, src/types/weather.ts, src/__tests__/openMeteo.spec.ts</files>
  <read_first>
    - src/lib/openMeteo.ts - ForecastResponse interface (lines 17-24) and fetchCurrentWeather (lines 58-79); also fetchForecast (lines 84-109) as the existing example of combining daily/forecast_days/timezone:'auto' params in one call
    - src/types/weather.ts - CurrentWeather (lines 27-33) and the field-comment convention used on DailyForecast/HourlyForecast (lines 39-54, e.g. "hourly precipitation, mm")
    - src/__tests__/openMeteo.spec.ts - the existing fetchCurrentWeather describe block (lines 83-125), MSW server.use(http.get(FORECAST_URL, ...)) pattern
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Pattern 3 and Code Example 3 (live-verified Open-Meteo response shape)
  </read_first>
  <behavior>
    - fetchCurrentWeather(lat, lon) sends ONE GET to FORECAST_URL with current='temperature_2m,relative_humidity_2m,weather_code,wind_speed_10m,apparent_temperature,precipitation,uv_index', wind_speed_unit='kmh' (unchanged), daily='sunrise,sunset', forecast_days=1, timezone='auto'.
    - Given a response with current: (temperature_2m, relative_humidity_2m, weather_code, wind_speed_10m, apparent_temperature, precipitation, uv_index) and daily: (sunrise: [iso], sunset: [iso]), the resolved CurrentWeather equals (temperature, humidity, weatherCode, windSpeed, feelsLike=apparent_temperature, precipitation, uvIndex=uv_index, sunrise=daily.sunrise[0], sunset=daily.sunset[0]).
    - A 500 response still rejects with response.status 500 (existing error-shape test must keep passing unchanged).
  </behavior>
  <action>
    In src/types/weather.ts, add to the CurrentWeather interface: feelsLike (number, feels-like temperature in Celsius, same unit toggle as temperature), precipitation (number, mm, no unit toggle), uvIndex (number, 0-11+ scale, no unit), sunrise (string, local ISO datetime, no offset), sunset (string, same). Match the inline-comment style already used on DailyForecast/HourlyForecast fields.
    In src/lib/openMeteo.ts, extend the ForecastResponse interface's current block with apparent_temperature (number), precipitation (number), uv_index (number), and add a new daily block to the same interface with sunrise (string array) and sunset (string array) - ForecastResponse now covers both current and daily since one request returns both. In fetchCurrentWeather, extend the current params string to include apparent_temperature, precipitation, uv_index after the existing four fields, add daily: 'sunrise,sunset', forecast_days: 1, timezone: 'auto' to the SAME params object passed to http.get (never a second call), destructure both current and daily from response.data, and return the extended object mapping feelsLike from apparent_temperature, precipitation from precipitation, uvIndex from uv_index, sunrise from daily.sunrise[0], sunset from daily.sunset[0], alongside the four existing mapped fields.
    In src/__tests__/openMeteo.spec.ts, extend the existing "normalizes the snake_case current object..." test (lines 84-115): assert the new current/daily/forecast_days/timezone query-param values via url.searchParams.get(...), extend the MSW HttpResponse.json(...) fixture to include apparent_temperature: 13.1, precipitation: 0.4, uv_index: 2.3 in current and a daily block with sunrise: ['2026-07-08T04:52'] and sunset: ['2026-07-08T19:47'], and extend the toEqual(...) assertion to include feelsLike: 13.1, precipitation: 0.4, uvIndex: 2.3, sunrise: '2026-07-08T04:52', sunset: '2026-07-08T19:47'. Leave the existing "rejects with the HTTP status on a 500" test unchanged - it must keep passing with the extended function.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/openMeteo.spec.ts</automated>
  </verify>
  <acceptance_criteria>
    - CurrentWeather interface in src/types/weather.ts has exactly nine fields: temperature, weatherCode, windSpeed, humidity, feelsLike, precipitation, uvIndex, sunrise, sunset.
    - fetchCurrentWeather issues exactly one http.get call per invocation (grep src/lib/openMeteo.ts for "http.get" inside fetchCurrentWeather shows one call site).
    - npx vitest run src/__tests__/openMeteo.spec.ts passes all fetchCurrentWeather tests, including the extended param and mapping assertions.
  </acceptance_criteria>
  <done>fetchCurrentWeather and CurrentWeather carry the five new fields, verified by an updated MSW test asserting both the request params and the response mapping.</done>
</task>

<task type="auto">
  <name>Task 2: Update the cityDetail.spec.ts fetchCurrentWeather fixture to the extended shape</name>
  <files>src/__tests__/cityDetail.spec.ts</files>
  <read_first>
    - src/__tests__/cityDetail.spec.ts - the vi.mock('@/lib/openMeteo') block (lines 20-42), specifically the fetchCurrentWeather.mockResolvedValue fixture (lines 29-34)
    - .planning/phases/07-richer-weather-milestone-verification/07-RESEARCH.md - Pitfall 5 (stale mock fixtures after extending fetchCurrentWeather's shape)
  </read_first>
  <action>
    In the vi.mock('@/lib/openMeteo') block's fetchCurrentWeather.mockResolvedValue fixture, add the five new fields alongside the existing temperature/weatherCode/windSpeed/humidity: feelsLike: 19, precipitation: 0, uvIndex: 3, sunrise: '2026-06-12T04:35', sunset: '2026-06-12T19:20'. Do not change any other part of this file in this task - Plan 07-05 will add assertions against a new current-conditions panel using this same fixture.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/cityDetail.spec.ts && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - cityDetail.spec.ts's fetchCurrentWeather mock fixture includes feelsLike, precipitation, uvIndex, sunrise, sunset with the values above.
    - npx vitest run src/__tests__/cityDetail.spec.ts passes (the existing test does not yet assert on the new fields, so this is a pure regression check).
    - npm run test (full suite) stays green.
  </acceptance_criteria>
  <done>cityDetail.spec.ts's mocked fetchCurrentWeather fixture matches the real function's extended return shape, so Plan 07-05's new panel assertions have real data to read.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02 scope)

- src/types/weather.ts: CurrentWeather interface gains feelsLike, precipitation, uvIndex, sunrise, sunset fields
- src/lib/openMeteo.ts: ForecastResponse interface gains apparent_temperature/precipitation/uv_index (current) and a new daily block (sunrise/sunset); fetchCurrentWeather return value gains the five new mapped fields
- No new files created in this plan

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|--------------|
| Open-Meteo API response -> app | third-party JSON is parsed into typed objects, never `any` |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-07-DATA | Tampering / Information Disclosure | Extended ForecastResponse parsing in openMeteo.ts | mitigate | Response fields typed locally (ForecastResponse), no `any`; values pass straight through to typed CurrentWeather fields, never interpolated into a query or executed - matches the existing pattern for temperature/humidity/wind. |
| T-07-PARAM | Tampering | Extended query params (apparent_temperature, precipitation, uv_index, daily) | accept | All params are fixed string literals chosen by this code, never derived from user input; passed through axios's `params` object (URL-encoded), matching every other call in this file. |
</threat_model>

<verification>
- Automated gate in Task 1 covers: request params, response mapping, existing error-shape regression.
- Automated gate in Task 2 covers: fixture shape parity with the real function, full-suite regression.
- Human check (deferred to end-of-phase per workflow.human_verify_mode=end-of-phase): none needed yet - no UI reads these fields until Plan 07-05.
</verification>

<success_criteria>
- fetchCurrentWeather and CurrentWeather carry all five new WTHR-04 fields, fetched in one HTTP call, verified by MSW tests against the live-verified Open-Meteo response shape.
- All existing tests that touch fetchCurrentWeather's mocked shape are updated so Plan 07-05 can build UI against real data immediately.
</success_criteria>

<output>
Create `.planning/phases/07-richer-weather-milestone-verification/07-02-SUMMARY.md` when done.
</output>
