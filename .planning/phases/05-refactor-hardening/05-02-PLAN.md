---
phase: 05-refactor-hardening
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - src/router/index.ts
  - src/pages/NotFoundPage.vue
  - src/components/CitySearch.vue
  - src/i18n/messages/en.ts
  - src/i18n/messages/ja.ts
autonomous: true
requirements: [NAV-04, NAV-05, SRCH-04]
must_haves:
  truths:
    - "Visiting an unknown URL (e.g. /this/does/not/exist) renders a friendly 404 page whose button navigates back to the dashboard"
    - "npm run build emits a separate JS chunk for each of the four pages (DashboardPage, CityDetailPage, SettingsPage, NotFoundPage)"
    - "Typing in city search fires exactly one geocode request 300ms after the last keystroke"
    - "A pending debounce never fires a geocode after CitySearch unmounts, and a new input aborts the in-flight request"
  artifacts:
    - path: src/router/index.ts
      provides: "Lazy routes + catch-all 404 route"
      contains: "pathMatch(.*)*"
    - path: src/pages/NotFoundPage.vue
      provides: "Friendly 404 page with dashboard link"
      min_lines: 15
    - path: src/components/CitySearch.vue
      provides: "useDebounceFn debounce with onScopeDispose cleanup"
      contains: "useDebounceFn"
  key_links:
    - from: src/router/index.ts
      to: src/pages/NotFoundPage.vue
      via: "lazy import on the catch-all route, placed LAST"
      pattern: "import\\('@/pages/NotFoundPage\\.vue'\\)"
    - from: src/pages/NotFoundPage.vue
      to: "dashboard route"
      via: "v-btn :to named route"
      pattern: ":to=\"\\{ name: 'dashboard' \\}\""
    - from: src/components/CitySearch.vue
      to: src/lib/openMeteo.ts
      via: "geocodeCity called with the AbortController signal"
      pattern: "geocodeCity\\(text, controller\\.signal\\)"
    - from: src/components/CitySearch.vue
      to: "@vueuse/core"
      via: "useDebounceFn wraps the geocode callback at 300ms"
      pattern: "useDebounceFn"
---

<objective>
Harden navigation and search: lazy-load every page route so each ships as its own chunk (NAV-04), add a catch-all 404 route plus a friendly NotFoundPage (NAV-05), and replace CitySearch's hand-rolled setTimeout debounce with VueUse `useDebounceFn` plus explicit unmount cleanup (SRCH-04).

Purpose: closes the v1.0 routing gaps and finishes the "delete hand-rolled code" theme - VueUse gets its one obvious, visible job in the app.
Output: reworked router with 4 lazy routes + catch-all, new NotFoundPage.vue, reworked CitySearch debounce, notFound i18n keys in both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refactor-hardening/05-RESEARCH.md
@.planning/phases/05-refactor-hardening/05-PATTERNS.md
@src/router/index.ts
@src/components/CitySearch.vue
</context>

## Conventions that apply to every task

- Teaching comments on every non-obvious line (project-wide study convention). Never use the em-dash character; use "-".
- i18n key parity: every key add happens in BOTH `en.ts` and `ja.ts`.
- Comments must NOT contain the literal strings used by verification gates below ("setTimeout", "pathMatch" outside the route definition itself, "v-html").
- Depends on plan 05-01 having landed (locale files already contain card.retry / detail.retry; card notFound key deleted).

<tasks>

<task type="auto">
  <name>Task 1: Create NotFoundPage.vue and the notFound i18n keys (NAV-05)</name>
  <files>src/pages/NotFoundPage.vue, src/i18n/messages/en.ts, src/i18n/messages/ja.ts</files>
  <read_first>
    - src/pages/CityDetailPage.vue (the in-page not-found branch, lines ~61-69 - structure/classes/i18n style analog)
    - src/i18n/messages/en.ts and ja.ts (detail.notFoundTitle / notFoundBody / backToDashboard - existing copy to adapt)
    - .planning/phases/05-refactor-hardening/05-PATTERNS.md - NotFoundPage section (exact template excerpt)
  </read_first>
  <action>
    Create `src/pages/NotFoundPage.vue` following the PATTERNS.md excerpt: a `<section class="pa-4">` with an `<h1 class="text-h4 mb-2">` showing t('notFound.title'), a `<p class="mb-4 text-medium-emphasis">` showing t('notFound.body'), and a `<v-btn :to="{ name: 'dashboard' }" color="primary" prepend-icon="mdi-arrow-left">` showing t('notFound.backToDashboard'). Script: `<script setup lang="ts">` with `useI18n()` and a teaching comment explaining that the router's catch-all route renders this page for any unknown URL.
    SECURITY (threat T-05-04): do NOT read or render `route.params.pathMatch` anywhere on this page, and never use v-html - the unknown path is user-controlled input.
    Add a top-level `notFound: { title, body, backToDashboard }` section to BOTH en.ts and ja.ts. Adapt the wording from the existing detail.notFoundTitle / detail.notFoundBody / detail.backToDashboard copy (e.g. en title "Page not found", body "That URL does not match anything here.", backToDashboard mirroring the existing dashboard-link copy; ja adapted from the existing ja equivalents).
  </action>
  <verify>
    <automated>npx vue-tsc --build && grep -q "notFound:" src/i18n/messages/en.ts && grep -q "notFound:" src/i18n/messages/ja.ts && test "$(grep -c 'backToDashboard:' src/i18n/messages/en.ts)" -eq 2 && test "$(grep -c 'backToDashboard:' src/i18n/messages/ja.ts)" -eq 2 && ! grep -q "pathMatch" src/pages/NotFoundPage.vue && ! grep -q "v-html" src/pages/NotFoundPage.vue</automated>
  </verify>
  <acceptance_criteria>
    - NotFoundPage.vue exists, typechecks, uses only t('notFound.*') strings, and links to the dashboard via the named route.
    - notFound.title / notFound.body / notFound.backToDashboard exist in BOTH locale files (backToDashboard grep count is exactly 2 per file: existing detail key + new notFound key).
    - The page never touches route params and contains no v-html.
  </acceptance_criteria>
  <done>404 page component and its locale keys exist in parity; typecheck green.</done>
</task>

<task type="auto">
  <name>Task 2: Lazy-load all page routes and append the catch-all (NAV-04, NAV-05)</name>
  <files>src/router/index.ts</files>
  <read_first>
    - src/router/index.ts (current static imports and route table - the file being modified)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - Code Example 3 and Pitfall 8 (lazy-route effect on app-mount specs)
    - src/__tests__/navigation.spec.ts (regression watch - it mounts App with the real router)
  </read_first>
  <action>
    Remove the three static page imports. Convert every route to a lazy component: `component: () => import('@/pages/DashboardPage.vue')` (same for CityDetailPage, SettingsPage). Lazy-load ALL pages including the dashboard - resolved research question: consistency wins and Vite modulepreloads the entry route's chunk anyway. Append the catch-all LAST: `{ path: '/:pathMatch(.*)*', name: 'not-found', component: () => import('@/pages/NotFoundPage.vue') }`. Do NOT wrap anything in defineAsyncComponent (lazy routes are a distinct mechanism). Keep BOTH exports: `export const router` AND `export default router` - existing tests import the default. Update the teaching comment block to explain route-level code splitting (one Vite chunk per page file) and the catch-all.
    Regression note (RESEARCH Pitfall 8): lazy components resolve async; existing specs already `await router.isReady()` + `flushPromises()`. If a navigation assertion turns flaky, the ONLY allowed spec edit is adding one extra `await flushPromises()` to the affected test - record it as a deviation in the summary.
  </action>
  <verify>
    <automated>npm run test && npm run build && test "$(ls dist/assets | grep -cE '^(DashboardPage|CityDetailPage|SettingsPage|NotFoundPage)-.*\.js$')" -eq 4 && grep -q "export default router" src/router/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - All four routes use `() => import(...)`; the catch-all is the LAST entry with name 'not-found'.
    - `npm run build` output contains exactly four page chunks named after the page files (NAV-04 observable proof).
    - Both named and default router exports remain.
    - Full test suite green (app-mount specs unaffected or fixed only by an added flushPromises).
  </acceptance_criteria>
  <done>Every page ships as its own chunk and unknown URLs resolve to the not-found route; suite green.</done>
</task>

<task type="auto">
  <name>Task 3: Replace CitySearch's setTimeout debounce with useDebounceFn plus manual cleanup (SRCH-04)</name>
  <files>src/components/CitySearch.vue</files>
  <read_first>
    - src/components/CitySearch.vue (KEEP: yup schema, useField with validateOnValueUpdate:false, suppressSearch echo guard, empty-input resetField early return, onSelect, cityTitle, the template's @update:search contract. REPLACE: the debounce id variable and the setTimeout body, lines ~36 and ~59-77)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - Code Example 4 and Pitfall 3 (verified: installed VueUse 14.3.0 useDebounceFn has NO .cancel() and NO auto scope-dispose cleanup)
    - .planning/phases/05-refactor-hardening/05-PATTERNS.md - CitySearch section
  </read_first>
  <action>
    Replace only the timer mechanics, exactly per RESEARCH Code Example 4:
    - Import `onScopeDispose` from 'vue' and `useDebounceFn` from '@vueuse/core'.
    - Module-level (setup scope) `let controller: AbortController | undefined` and `let disposed = false`; register `onScopeDispose(() => { disposed = true; controller?.abort() })` with a teaching comment: the debounce timer can still fire after unmount because useDebounceFn provides no cancel, so we flag disposal and abort in-flight work - this IS the proper cleanup SRCH-04 requires.
    - `const debouncedGeocode = useDebounceFn(async (text: string) => { ... }, 300)` whose body: early-return if `disposed`; `await validate()` and on invalid set items to [] and return; `controller?.abort()` (abort-on-new-input - only the latest term resolves) then `controller = new AbortController()`; set loading true; `items.value = await geocodeCity(text, controller.signal)` in try, `items.value = []` in catch, loading false in finally.
    - In `onSearch`, keep the suppressSearch echo guard and the empty-input resetField early return unchanged; the tail becomes `debouncedGeocode(text)`.
    - Update the old "hand-rolled - no lodash" teaching comment to name useDebounceFn as VueUse's visible job here. Do NOT rely on any `.cancel()`/`.flush()` member - the installed 14.3.0 return type has none (verified against installed source; docs claiming otherwise were discarded).
  </action>
  <verify>
    <automated>! grep -q "setTimeout" src/components/CitySearch.vue && grep -q "useDebounceFn" src/components/CitySearch.vue && grep -q "onScopeDispose" src/components/CitySearch.vue && grep -q "geocodeCity(text, controller.signal)" src/components/CitySearch.vue && npx vue-tsc --build && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - No setTimeout/clearTimeout debounce bookkeeping remains in CitySearch.vue.
    - useDebounceFn wraps the geocode callback at 300ms; onScopeDispose sets a disposed flag and aborts the controller.
    - The debounced body aborts the previous controller before creating a new one (abort-on-new-input preserved).
    - suppressSearch guard, empty-input reset, onSelect, and the template contract are untouched; typecheck and full suite green.
  </acceptance_criteria>
  <done>CitySearch debounces via VueUse with real unmount cleanup; all existing behavior intact.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 02 scope)

- `src/pages/NotFoundPage.vue` - new page component (template: notFound.title heading, notFound.body text, dashboard v-btn)
- Router route `{ path: '/:pathMatch(.*)*', name: 'not-found' }` plus four lazy route components (src/router/index.ts)
- Build artifacts: `dist/assets/DashboardPage-*.js`, `CityDetailPage-*.js`, `SettingsPage-*.js`, `NotFoundPage-*.js` (one chunk per page)
- CitySearch.vue internals: `debouncedGeocode` (useDebounceFn, 300ms), `disposed` flag + `onScopeDispose` cleanup, AbortController threading
- i18n keys added in BOTH locales: `notFound.title`, `notFound.body`, `notFound.backToDashboard`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL -> catch-all route -> NotFoundPage | fully user-controlled unknown path reaches the app |
| Search input -> debounce -> Open-Meteo geocoding API | user keystrokes drive outbound requests |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-04 | Tampering (XSS) | NotFoundPage | mitigate | Never render `route.params.pathMatch`; no v-html; static i18n copy only (grep-gated in Task 1). |
| T-05-05 | Denial of Service | geocoding request rate | mitigate | 300ms debounce retained via useDebounceFn; abort-on-new-input keeps at most one in-flight geocode. |
| T-05-06 | Denial of Service (resource leak) | CitySearch after unmount | mitigate | onScopeDispose disposed flag + controller.abort() stops post-unmount timers/requests. |
</threat_model>

<verification>
- `npm run test` fully green; `npm run build` emits exactly four page chunks (NAV-04 proof lives in the build output).
- Grep gates: catch-all present and last, no setTimeout debounce, no pathMatch/v-html in NotFoundPage.
- Human check (deferred to end-of-phase verification per workflow.human_verify_mode=end-of-phase): visit an unknown URL in the dev server, see the friendly 404, click the button back to the dashboard; type quickly in city search and watch the network tab show a single geocode request ~300ms after the last keystroke.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 3 satisfied: unknown URL shows a friendly 404 with a working dashboard link; each page is its own lazy chunk, visible in build output.
- ROADMAP criterion 4 (code half) satisfied: debounce via useDebounceFn with cleanup on unmount (tests land in plan 05-04).
- Requirements NAV-04, NAV-05, SRCH-04 implemented and recorded in frontmatter.
</success_criteria>

<output>
Create `.planning/phases/05-refactor-hardening/05-02-SUMMARY.md` when done.
</output>
