---
phase: 05-refactor-hardening
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/composables/useForecast.ts
  - src/composables/useCurrentWeather.ts
  - src/pages/CityDetailPage.vue
  - src/components/WeatherCard.vue
  - src/i18n/messages/en.ts
  - src/i18n/messages/ja.ts
autonomous: true
requirements: [DATA-04, DATA-05, WTHR-03]
must_haves:
  truths:
    - "Deep-linking or reloading /city/:id loads that city's data with no fetch firing before the city resolves - no latitude=0/longitude=0 request ever occurs"
    - "CityDetailPage contains no Proxy hack and no sentinel city object"
    - "A failed weather or forecast load shows the generic load-error copy (never 'city not found') plus a retry button that refetches"
    - "Clicking retry on a WeatherCard does not navigate to the detail page"
    - "Existing cityDetail.spec.ts passes with zero edits, proving DATA-04 preserved behavior"
  artifacts:
    - path: src/composables/useForecast.ts
      provides: "Reactive forecast query with enabled guard"
      contains: "MaybeRefOrGetter<SavedCity | undefined>"
    - path: src/composables/useCurrentWeather.ts
      provides: "Reactive current-weather query with enabled guard"
      contains: "MaybeRefOrGetter<SavedCity | undefined>"
    - path: src/pages/CityDetailPage.vue
      provides: "Reactive city wiring + detail retry button"
    - path: src/components/WeatherCard.vue
      provides: "Getter prop wiring + card retry button, dead 404 branch removed"
  key_links:
    - from: src/components/WeatherCard.vue
      to: src/composables/useCurrentWeather.ts
      via: "getter argument keeps the prop reactive"
      pattern: "useCurrentWeather\\(\\(\\) => props\\.city\\)"
    - from: src/pages/CityDetailPage.vue
      to: src/composables/useForecast.ts
      via: "existing city computed passed directly"
      pattern: "useForecast\\(city\\)"
    - from: src/composables/useForecast.ts
      to: src/lib/openMeteo.ts
      via: "enabled guard gates fetchForecast"
      pattern: "enabled: computed\\(\\(\\) => !!toValue\\(city\\)\\)"
    - from: src/components/WeatherCard.vue
      to: "useCurrentWeather refetch"
      via: "retry button with stop+prevent so the card router-link does not navigate"
      pattern: "@click\\.stop\\.prevent=\"refetch"
---

<objective>
Make the weather and forecast composables reactive with an `enabled` guard (DATA-04), delete the CityDetailPage Proxy hack and its lat-0/lon-0 "null island" fetch, add retry buttons to both error states (DATA-05), and remove WeatherCard's dead 404 "city not found" error branch (WTHR-03).

Purpose: this is the foundation refactor of Phase 5. Every later v1.1 feature (hourly chart, geolocation, refresh) builds on the reactive composable pattern instead of the old Proxy workaround (see STATE.md blocker).
Output: reworked `useForecast` / `useCurrentWeather`, cleaned CityDetailPage and WeatherCard, retry i18n keys in both locales, dead key removed from both locales.
</objective>

<execution_context>
@$HOME/.claude/gsd-core/workflows/execute-plan.md
@$HOME/.claude/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-refactor-hardening/05-RESEARCH.md
@.planning/phases/05-refactor-hardening/05-PATTERNS.md
@src/composables/useForecast.ts
@src/composables/useCurrentWeather.ts
@src/pages/CityDetailPage.vue
@src/components/WeatherCard.vue
</context>

## Conventions that apply to every task

- Teaching comments on every non-obvious line, naming the library's role ("Vue Query = server state..."). This is the project-wide study convention (05-PATTERNS.md Shared Patterns).
- Never use the em-dash character in code or comments; use "-".
- Import order: vue -> library -> `@/lib` -> `@/types`.
- i18n key parity: every key add/delete happens in BOTH `en.ts` and `ja.ts` (hard convention).
- Comments must NOT contain the literal strings used by verification gates below ("new Proxy", "isAxiosError", "card.notFound", "latitude: 0"). Describe the old code indirectly (e.g. "the old Proxy-based workaround").

<tasks>

<task type="auto">
  <name>Task 1: Rework useForecast and useCurrentWeather to a reactive city signature (DATA-04)</name>
  <files>src/composables/useForecast.ts, src/composables/useCurrentWeather.ts</files>
  <read_first>
    - src/composables/useForecast.ts (current shape - the file being modified)
    - src/composables/useCurrentWeather.ts (same shape)
    - .planning/phases/05-refactor-hardening/05-RESEARCH.md - Code Example 1 (the verified target implementation) and Pitfalls 1-2
    - src/lib/openMeteo.ts (exact fetchForecast / fetchCurrentWeather signatures, signal param)
    - src/types/weather.ts (SavedCity shape, the `key` field)
  </read_first>
  <action>
    Change both composables' parameter from `city: SavedCity` to `city: MaybeRefOrGetter<SavedCity | undefined>` (import `computed`, `toValue`, `type MaybeRefOrGetter` from 'vue'). Per RESEARCH Code Example 1 (verified against installed @tanstack/vue-query 5.101.0 types):
    - `queryKey: computed(() => ['forecast', toValue(city)?.key])` in useForecast; `['currentWeather', toValue(city)?.key]` in useCurrentWeather. Key roots MUST stay 'forecast' / 'currentWeather' (cache identity unchanged).
    - queryFn: `const c = toValue(city); if (!c) throw new Error('...: query must stay disabled without a city')` then call `fetchForecast(c.latitude, c.longitude, 7, signal)` / `fetchCurrentWeather(c.latitude, c.longitude, signal)` with the queryFn's destructured `{ signal }`. Do NOT use a `!` non-null assertion (RESEARCH Pitfall 1 - the guard-throw teaches why enabled exists).
    - `enabled: computed(() => !!toValue(city))` - this is the gate that kills the null-island fetch.
    - Keep `staleTime: 5 * 60 * 1000` unchanged in both.
    Update the teaching comments to explain `enabled` + `toValue` (why the query stays off until a city resolves, why the key is computed). Note: existing call sites still compile because a plain SavedCity value is assignable to MaybeRefOrGetter, so this task lands green on its own.
  </action>
  <verify>
    <automated>npx vue-tsc --build && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - Both composables accept `MaybeRefOrGetter<SavedCity | undefined>` (grep finds the string in both files).
    - Both contain an `enabled:` guard built from `toValue(city)`.
    - Query key roots 'forecast' and 'currentWeather' and staleTime 5 * 60 * 1000 are unchanged.
    - No `!` non-null assertion on the city inside queryFn (guard-throw instead).
    - `npx vue-tsc --build` passes and the full existing suite (`npm run test`) stays green with zero test edits.
  </acceptance_criteria>
  <done>Both composables are reactive with enabled guards; typecheck and full suite green.</done>
</task>

<task type="auto">
  <name>Task 2: Delete the CityDetailPage Proxy hack, pass the city computed, add detail retry (DATA-04, DATA-05)</name>
  <files>src/pages/CityDetailPage.vue, src/i18n/messages/en.ts, src/i18n/messages/ja.ts</files>
  <read_first>
    - src/pages/CityDetailPage.vue (lines 21-24 city computed to KEEP; lines 34-53 Proxy hack to DELETE; lines 61-69 in-page not-found branch to KEEP; line 80 error alert)
    - .planning/phases/05-refactor-hardening/05-PATTERNS.md - CityDetailPage section (exact delete/keep line map)
    - src/i18n/messages/en.ts and src/i18n/messages/ja.ts (detail section, existing copy style)
  </read_first>
  <action>
    DELETE the whole Proxy workaround block (currently lines 34-53): the `queryCity` ref with the `{ latitude: 0, longitude: 0 }` sentinel, the `watch` that copies into it, and the Proxy argument passed to `useForecast`. Remove now-unused imports (`watch`, `ref`, `Ref`) from the vue import line. KEEP the existing `city` computed (route.params.id lookup against store.cities) and pass it straight in: `const { data, isPending, isError, refetch } = useForecast(city)`. KEEP the in-page not-found branch (known route, unknown saved city - distinct from the router 404 coming in plan 02). The route param continues to be used ONLY for store lookup, never to build request coordinates (threat T-05-01).
    Add the retry button (DATA-05) inside the existing error `v-alert` (keep its exact shell: `type="error" variant="tonal" density="compact"`): a `<template #append>` with `<v-btn size="small" variant="text" @click="refetch()">{{ t('detail.retry') }}</v-btn>`. Add key `detail.retry` to BOTH en.ts ("Retry") and ja.ts (short imperative matching existing ja copy style, e.g. the style of detail.backToDashboard). Add a teaching comment noting refetch comes free from Vue Query.
  </action>
  <verify>
    <automated>! grep -q "new Proxy" src/pages/CityDetailPage.vue && ! grep -q "latitude: 0" src/pages/CityDetailPage.vue && grep -q "useForecast(city)" src/pages/CityDetailPage.vue && npm run test && test -z "$(git status --porcelain src/__tests__)"</automated>
  </verify>
  <acceptance_criteria>
    - No Proxy construction and no lat-0/lon-0 sentinel remains in CityDetailPage.vue.
    - `useForecast(city)` receives the existing computed directly; `refetch` is destructured and wired to a retry button showing t('detail.retry').
    - `detail.retry` exists in BOTH locale files.
    - The in-page not-found branch is untouched.
    - Full suite green AND `git status --porcelain src/__tests__` is empty (cityDetail.spec.ts unchanged - behavior preserved).
  </acceptance_criteria>
  <done>CityDetailPage uses the reactive composable directly, retry works, dead workaround deleted, suite green with zero test edits.</done>
</task>

<task type="auto">
  <name>Task 3: WeatherCard - getter prop, remove dead 404 branch, add card retry (WTHR-03, DATA-05, DATA-04)</name>
  <files>src/components/WeatherCard.vue, src/i18n/messages/en.ts, src/i18n/messages/ja.ts</files>
  <read_first>
    - src/components/WeatherCard.vue (line 3 axios import to DELETE; line 22 destructure; lines 33-41 dead errorMessage computed to DELETE; line 52 router-link card root; line 63 existing @click.stop convention on the remove button; lines 73-74 error alert)
    - .planning/phases/05-refactor-hardening/05-PATTERNS.md - WeatherCard section
    - src/i18n/messages/en.ts line ~40 and ja.ts line ~27 (the card notFound key to delete)
  </read_first>
  <action>
    Three changes per the pattern map:
    1. Reactive prop (DATA-04 / RESEARCH Pitfall 2): change `useCurrentWeather(props.city)` to `useCurrentWeather(() => props.city)` - a getter, so a changed prop re-keys the query. Destructure `refetch`; drop `error` (no longer used).
    2. Dead branch removal (WTHR-03): delete the `errorMessage` computed (the axios-404 "city not found" mapping) and the `isAxiosError` import from 'axios'. In the template, replace `{{ errorMessage }}` with `{{ t('card.loadError') }}` - generic copy only, never the raw error object (threat T-05-02). Delete the card-section notFound key from BOTH en.ts and ja.ts (grep first to confirm WeatherCard is still its only user - it was at research time).
    3. Retry button (DATA-05): in the error `v-alert` (keep its exact shell), add `<template #append>` with `<v-btn size="small" variant="text" @click.stop.prevent="refetch()">{{ t('card.retry') }}</v-btn>`. The `.stop.prevent` is REQUIRED: the card root is a router-link v-card, and retry must not navigate (copy the existing @click.stop convention from the remove button, plus .prevent). Add `card.retry` to BOTH locales (en "Retry", ja matching existing style).
    Update teaching comments (why the getter, why the generic error copy is now accurate - Open-Meteo signals "not found" via empty geocoder results, not a coordinate 404).
  </action>
  <verify>
    <automated>! grep -rq "card\.notFound" src/ && ! grep -q "isAxiosError" src/components/WeatherCard.vue && ! grep -q "notFound:" src/i18n/messages/en.ts && ! grep -q "notFound:" src/i18n/messages/ja.ts && grep -q "useCurrentWeather(() => props.city)" src/components/WeatherCard.vue && grep -q '@click.stop.prevent="refetch()"' src/components/WeatherCard.vue && test "$(grep -c 'retry:' src/i18n/messages/en.ts)" -eq 2 && test "$(grep -c 'retry:' src/i18n/messages/ja.ts)" -eq 2 && npx vue-tsc --build && npm run test</automated>
  </verify>
  <acceptance_criteria>
    - WeatherCard passes a getter to useCurrentWeather; no axios error-status inspection remains.
    - The dead notFound key is gone from BOTH locale files; `card.retry` and `detail.retry` exist in BOTH (grep count for "retry:" is exactly 2 per file).
    - Retry button uses `@click.stop.prevent="refetch()"` (no navigation on retry).
    - Typecheck and full suite green.
  </acceptance_criteria>
  <done>WeatherCard shows accurate error copy with a working, non-navigating retry; locale files are in parity.</done>
</task>

</tasks>

## Artifacts this phase produces (plan 01 scope)

- `useForecast(city: MaybeRefOrGetter<SavedCity | undefined>)` - reworked signature, computed queryKey `['forecast', key]`, enabled guard (src/composables/useForecast.ts)
- `useCurrentWeather(city: MaybeRefOrGetter<SavedCity | undefined>)` - same pattern, key `['currentWeather', key]` (src/composables/useCurrentWeather.ts)
- CityDetailPage.vue: Proxy workaround deleted; `refetch` wired to a detail retry button
- WeatherCard.vue: getter prop wiring, dead 404 branch deleted, card retry button
- i18n keys added in BOTH locales: `card.retry`, `detail.retry`; key deleted in BOTH: card-section `notFound`

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Browser URL -> router -> CityDetailPage | user-controlled `:id` route param enters app logic |
| App -> Open-Meteo API | untrusted response data and failure modes |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-05-01 | Tampering | CityDetailPage `route.params.id` | mitigate | Param used only as a lookup key against saved cities in the Pinia store; never interpolated into request URLs or coordinates. Preserve this property through the DATA-04 refactor (ASVS V5). |
| T-05-02 | Information Disclosure | error alerts (WeatherCard, CityDetailPage) | mitigate | Render only static i18n copy; never render the raw error object. |
| T-05-03 | Denial of Service | composable queryFn | mitigate | `enabled` guard stops requests while no city is resolved - no sentinel-coordinate fetches to Open-Meteo. |
</threat_model>

<verification>
- `npx vue-tsc --build` clean; `npm run test` fully green with `git status --porcelain src/__tests__` empty (no spec edits - cityDetail.spec.ts proves DATA-04 preserved behavior).
- Grep gates: no Proxy construction, no lat-0 sentinel, no axios-404 branch, no dead notFound key anywhere in src/.
- Human check (deferred to end-of-phase verification per workflow.human_verify_mode=end-of-phase): run dev server, deep-link /city/:id with the network tab open - confirm no latitude=0&longitude=0 request; toggle devtools offline to see the error alert, click retry after going back online and watch the refetch succeed.
</verification>

<success_criteria>
- ROADMAP Phase 5 success criterion 1 satisfied: deep-link/reload loads city data, no premature fetch, Proxy hack gone.
- ROADMAP criterion 2 satisfied: accurate error copy (no false "city not found") + working retry on both card and detail.
- Requirements DATA-04, DATA-05, WTHR-03 implemented and requirement IDs recorded in frontmatter.
</success_criteria>

<output>
Create `.planning/phases/05-refactor-hardening/05-01-SUMMARY.md` when done.
</output>
